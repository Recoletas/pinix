# Markdown 硬换行（行尾两个空格）是合法约定；EOF 空行同理不作为错误
*.md whitespace=-blank-at-eol,-blank-at-eof
# 许可证原文保持原样，不做空白检查
OFL.txt -whitespace
src/assets/fonts/OFL.txt -whitespace
# Gradle 的 Windows wrapper 由 Capacitor 生成并保留 CRLF。
*.bat text eol=crlf whitespace=cr-at-eol
