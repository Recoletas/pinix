PORT=3001
OPENCLAW_BASE_URL=http://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=
OPENCLAW_AGENT_ID=main

# Capacitor device builds: Pinax Express/provider origin, never a model API key.
# Use HTTPS outside an explicitly controlled local debug build.
VITE_PINAX_API_ORIGIN=
VITE_PINAX_ALLOW_CLEARTEXT=false
