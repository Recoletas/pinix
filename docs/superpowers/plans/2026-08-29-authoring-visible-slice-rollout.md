# Authoring 真实页面可见切片推进计划

**日期：** 2026-08-29

**状态：** V1–V4 已完成可见切片验收；V5 按“落笔上下文闭环 P1”推进，C1-0、C1-1A、C1-1B、C1-2、C1-3 与当前 writingUnit 重写切片已收口，下一刀为 C1-6 放弃/保存失败/迟到结果/观察器的集中故障注入

**上位产品真源：** [Authoring 文本工作台 v3](./2026-08-25-authoring-text-workbench-v3.md)

**已完成可靠性前置：** [Authoring 前端可靠性与真实用户模拟计划](./2026-08-28-authoring-frontend-reliability-and-user-simulation.md)
**视觉执行方法：** [视觉对齐工作流](../../engineering/visual-alignment-workflow.md)

## 0. 计划要纠正的问题

上一轮确实修复了 Ghost、长推演、记忆、撤销、批注和跨章作用域等真实错误，但实施切片横跨状态机、数据事务、组件接线、CSS 和测试，导致“内部改动很多，真实页面第一眼变化很少”。甚至 Authoring 最终样式仍把核心 `flex/grid` 骨架寄托在旧 scoped 样式上，运行态样式层失效时整页退化为普通文档流。

本计划不再以“完成多少函数、接通多少 props、增加多少测试”衡量进展。唯一推进单位是：

```text
一张真实页面
+ 一个作者任务
+ 一组可见状态
+ 一次截图确认
+ 一条可操作闭环
```

服务层只在当前可见闭环缺少数据或事务时修改。尚未进入当前截图的后台能力不扩建。

## 1. 与旧计划的关系

本计划不推翻已经正确的底层工作：

- 保留 schema v3 writingUnit、稳定 `unitId + nodeId`、批注迁移、历史与恢复；
- 保留唯一交互 owner、IME 门禁、cursor-dwell、Ghost 事务和可编辑长推演；
- 保留世界书、当前场、大纲、上下文 compiler 和 observer 的既有真源；
- 保留 AppShell workspace tabs、现有图标、字体、token 和可复用编辑器；
- 保留素材、画布、Experience 的独立完整页面，但不让它们占据文本工作台主流程。

本计划替代旧计划中宽泛的“Phase 0 真实原型”“Phase 2 左树与稿面”“Phase 12 视觉收口”和可靠性计划 Slice 4/5 的剩余视觉实施方式。旧文件继续保存数据合同与历史决策，不再拿它们的大阶段直接开工。

## 2. 用户反馈转成八条硬约束

1. **正文永远是最大舞台。** 页面第一眼必须先看到章节标题和正文，不先看到卡片、统计、Agent 状态或工具说明。
2. **宽屏保持稳定横向结构。** 左导航、中央稿面、右工具 rail 和已打开详情同处一行；正文滚动不能带着左右栏移动。
3. **只有文本锚定层跟随正文。** Ghost、命令菜单、选区工具、批注位置和长推演锚点可随目标文本重定位；右栏壳、大纲、设定、现场概览不能随正文滚动。
4. **章节与中文排版朴素。** 使用“第一章　魔力异常”，不加 `01`、协议号、英文微标或装饰性章节卡；标题必须位于稿面内部。
5. **writingUnit 不是自然段换皮。** 一个单元可含一段或数段完整文字；常态不把每段画成块，只有 hover、选中、拖动、批注或生成时才显露单元边界。
6. **短 Ghost 与长推演是两种清晰工具。** 短 Ghost 贴 caret、轻量、可渐进采纳；长推演位于目标 writingUnit 下方，可修改后再纳入，不能变成右侧聊天卡或整块突兀表单。
7. **当前场必须串起世界书和推演。** 左侧显示简报；点击在右侧打开详情；作者可从绑定世界书添加人物/地点，再以此条件推演当前片段，采纳后更新正文和明确选择的现场变化。
8. **公开工具和视觉证据都必须真实。** 批注、大纲、设定、现场、历史和上下文保留；角色并入当前场，素材移出默认 rail；没有闭环的入口先隐藏。每个切片只做一个代表区域，截图方向未确认就不向整页扩散。

## 3. 当前真实页面审计结论

| 可见区域 | 当前 owner | 可复用能力 | 主要问题 |
| --- | --- | --- | --- |
| AppShell 页签 | `WorkspaceTabs.vue` / route adapter | 已支持书与完整功能表面 | Authoring 内又有一层书页签，视觉和信息重复 |
| Authoring 顶栏 | `Authoring.vue` + 三份页面 CSS | 撤销、格式、排版、打字机、聚焦、专注、查找、取名 | 多层 CSS 争夺结构；工具过多时换行或裁切；素材入口挤占核心动作 |
| 左导航 | `Authoring.vue` | 构思文档、快速落笔、正文章节、世界书绑定 | 密度与层级仍不稳定；当前场和章节树争空间 |
| 当前场简报 | `AuthoringSceneRail.vue` | 时间、人物、地点、事件，已有详情/调整/推进事件 | 信息存在但没有形成“加角色 → 推演 → 纳入”的显性任务链 |
| 中央稿面 | `WritingNotebookEditor.vue` | writingUnit、排版、Ghost、命令、选区、批注、历史 | 标题、正文、单元边界、placeholder 和底栏尚未形成统一阅读节奏 |
| 长推演 | `AuthoringBlockComposer.vue` / `AuthoringBlockDraft.vue` | 已可编辑、恢复原稿、采用、丢弃 | 仍带工具卡片气质，与正文排版/留白融合不足 |
| 右 rail | `AuthoringWorkspaceToolRail.vue` | 九个入口均有不同程度实现 | 角色、素材重复领域入口；双栏是布局动作却混入内容工具 |
| 右详情 | `Authoring.vue` + authoring components | 批注、大纲、设定、现场、历史、AI context | 每个面板结构语言不同；详情、列表、状态和动作层级未统一 |
| CSS | `Writing.scoped.css` / `Writing.global.css` / `Authoring.block-native.css` | 大量既有视觉规则 | 同 selector、同断点多 owner；Authoring.vue 超过一万行，视觉修改容易波及逻辑 |

## 4. 冻结后的页面构图

### 4.1 桌面 1440px 及以上

```text
AppShell：项目/书/完整功能页签（唯一页签 owner）
┌──────────────────────────────────────────────────────────────┐
│ Authoring 工具条：撤销 格式 排版 取名 打字机 聚焦 专注 查找 更多 │ 42–44px
├──────────────┬──────────────────────────┬────┬───────────────┤
│ 构思 / 章节   │                          │工具│  当前工具详情  │
│ 220–252px    │      连续中央稿面         │44px│  300–340px     │
│              │                          │    │               │
│              │      正文唯一滚动         │固定│   内部滚动     │
├──────────────┤                          │    │               │
│ 当前场简报    │                          │    │               │
└──────────────┴──────────────────────────┴────┴───────────────┘
```

- AppShell workspace tab 是“打开哪本书/哪个完整功能页”的唯一 owner；Authoring 内部重复书页签在路由同步确认后退役。
- 左右侧使用接近白色的工作面，中央外围使用很浅的蓝灰，稿面与周边只保持小幅明度差，不做反色大块。
- 中央正文列不缩成过窄卡片；1440px 下正文有效行宽以约 42–50 个中文字符为基准，剩余空间用于呼吸和文本锚定层。
- 右详情关闭时正文获得空间；打开时不让正文小于可读下限。双栏只作为检查器头部布局动作，不占一个永久 rail 图标。

### 4.2 1024px

- 左栏收至约 200–220px；正文保持可读宽度；工具 rail 仍固定。
- 右详情采用覆盖式阅读台，贴 rail 左侧出现，不重排正文、不清空 selection、不改变 scrollTop。
- 当前场简报仍留左下；若高度不足，只让章节列表内部滚动，不把当前场推走。

### 4.3 390px

- 正文是唯一默认面；章节树、当前场和右详情都是互斥 sheet。
- 顶部工具保持单行横向访问；仅 200% zoom 的极窄有效视口允许可访问换行。
- 工具 rail 固定底部；打开 sheet 不重建编辑器、不丢 caret。
- 不把桌面三栏按比例缩小，不让章节列表和正文同时纵向串联。

## 5. 统一视觉与排版基线

### 5.1 顶栏

- 两层最多：AppShell 页签层 + 紧贴其下的 Authoring 工具层；中间不插空白带或重复书名卡。
- 工具按作者频率分组：`撤销/重做`、`粗体/斜体/分隔线`、`排版/取名`、`打字机/聚焦/专注`、`查找`、`更多`。
- 素材收件箱、完整素材库、导出、回到冒险进入“更多”或独立 workspace tab，不长期挤压写作工具。
- 保存状态只在状态变化后短暂显示“已保存”或失败；稳定保存态不永久占位。

### 5.2 稿面

- 标题位于稿面内部，距工具栏视觉间隔约 36–52px；不使用原生输入框边框。
- 正文章节标题为 `第一章　章名`；探索文档只显示文档名，不伪装章号。
- 正文默认物理字号目标约 17–18px，行高约 1.8–1.95；具体值以 Windows 中文实图确认，不用 CSS 数字自行宣布好看。
- 普通正文首行两字缩进；标题、列表、引文、空段、分隔线不缩进。
- 中文 `“”「」『』`、标点避头尾、选区和 IME 必须在真实字体下检查。
- 底栏与中央背景同色，默认只留字数/字符/修订；“已保存”仅短暂出现，不永久写“已签”。

### 5.3 writingUnit

- 一个 writingUnit 表示可被移动、批注、推演、回溯和引用的完整写作片段，可包含一至数个自然段。
- 常态无卡片、无整块底色、无持续边框；段落之间由排版节奏表达。
- hover 只在页边出现轻刻度；当前 unit 只用低强度页边信号；拖动/拆分时才显完整边界和落点。
- 新生成多段正文默认组成一个 writingUnit，不按自然段自动拆成多个块。

### 5.4 同屏强调预算

同一时刻只允许一个主强调：

1. Ghost 或长推演草稿；
2. 当前选区/批注；
3. 当前 writingUnit；
4. 世界书词汇匹配。

顺序靠前者出现时，后面的强调必须降低。不得让蓝底当前行、蓝色 unit rail、蓝色 Ghost、黄色设定词和批注选区同时争夺正文。

## 6. 代表性真实 fixture 与证据规则

所有切片复用同一组本地虚构数据，不用空页面判断成品：

- 书：《雾港纪事》；
- 5 个正文章节，当前第五章约 3500–5000 中文字；
- 3 个探索文档：未编排速记、莉娜视角、艾德加视角；
- 世界书人物：莉娜、艾德加；地点：旧港税务所；规则：星图回应；
- 当前 caret 位于第五章中段一个含 3 个自然段的 writingUnit；
- 当前场初始有人物莉娜、地点税务所、一个未决事件；
- 至少 2 条批注、1 个大纲节点、3 个章节快照；
- 提供 short Ghost、long draft、provider error、stale 五种可控状态。

成功截图预算：

- 每个切片最多保留 3 张评审图：1440 主状态、1024 关键重排、390 关键 sheet；
- 只有涉及文本锚点的切片额外允许 1 张局部放大图；
- 每张优先裁到必要区域，单图建议不超过 1.5MB；
- 用户确认后清理失败图和过期图；过程截图不入 Git；
- 自动几何报告与截图分开，截图只判断层级、密度、排版和协调性。

这里的“最多保留”不是限制校对次数。实现期间可以反复截图、比较和修正，直到功能通过；每轮只保留基线图、当前最新版和必要失败证据，上一轮已被替代的截图立即清理。

### 6.1 每个功能都必须走完的验收合同

任何按钮、菜单、面板、编辑动作或自动状态，都必须逐一完成下面四层，不能因为所属大切片整体可用就跳过：

| 层 | 必须回答的问题 | 最低证据 |
| --- | --- | --- |
| UI | 入口在哪里；默认、hover、focus、active、disabled 如何区分；与相邻组件是否同一基线、密度和视觉语言 | 当前功能的真实页面截图，含入口和展开结果 |
| 完整性 | empty/loading/ready/error/stale/success 是否齐全；主动作、取消、返回、撤销/恢复、持久化是否形成闭环 | 状态清单 + 一条真实作者任务 |
| 鲁棒性 | 长文本、快速连点、切书/切章、请求迟到、保存失败、重复打开、Escape、IME、zoom、窄屏是否安全 | 边界操作结果 + 必要失败截图/计数 |
| 协调性 | 打开和关闭是否改变正文宽度、scrollTop、caret、selection；是否与 Ghost、批注、当前场或其他 sheet 争 owner | 前后几何记录 + 校对后的代表截图 |

单功能状态使用以下进度，不允许从“已实现”直接跳到“完成”：

```text
未开始
→ 真实页面原型
→ 主路径可操作
→ 完整状态补齐
→ UI 第一次校对
→ 鲁棒性边界通过
→ UI 第二次校对
→ 用户截图确认
→ 完成
```

若用户在截图确认后指出问题，该功能退回“UI 第一次校对”或“完整状态补齐”，修正并再次截图；不限校对轮数，不能以已跑过测试为由拒绝视觉返工。

### 6.2 逐功能截图与反复校对闭环

每个功能的实现循环固定为：

1. **基线截图**：使用真实 fixture 截取功能入口、目标内容和周边关系，标出当前问题；
2. **结构原型**：只完成信息编排、位置、尺寸和主动作，截图判断方向；
3. **第一次校对**：逐项检查对齐、留白、字体、颜色、层级、点击热区、正文占比和与左右栏联系；
4. **完整状态补齐**：实现 empty/loading/ready/error/stale/disabled/success 中适用的状态；
5. **鲁棒性校对**：用长内容、慢请求、失败、快速切换、重复操作、缩放和窄屏实际操作；
6. **第二次视觉校对**：边界状态加入后重新看主页面，确认没有为了错误提示或 loading 把成品布局撑坏；
7. **用户截图验收**：提交当前功能的 1440 主图和确有重排价值的 1024/390 图，等待具体反馈；
8. **按反馈返工**：只修当前功能和必要相邻关系，再次截图；重复 3–8 直到确认；
9. **收口**：记录最终状态、删除过期图、再运行所属切片的一次 focused Gate。

截图校对不是“看起来差不多”。每轮必须逐项对照：

- 主内容是否仍是第一视觉焦点；
- 顶栏、正文、左右栏基线是否连续；
- 是否出现廉价卡片、重复标题、无意义状态胶囊或大面积底色；
- 中文标题、正文、说明、数字和图标是否各用正确字体角色；
- hover/focus/selected 是否能区分但不过度强调；
- loading/error/empty 是否改变稳定布局；
- 打开、关闭、切换后正文宽度、scrollTop、caret 和 selection 是否保持；
- 1440、1024、390 和 200% zoom 是否是重新编排，而非机械缩放；
- 截图中是否还有本轮未处理但已被改动波及的区域；
- 与上一轮截图比较，问题是被真正解决，还是被新覆盖规则暂时遮住。

### 6.3 逐功能实施与验收台账

下面各表是 66 项功能的冻结范围清单。实施某个 Slice 时，把该 Slice 涉及的 ID 复制到同一文件的“当前验收记录”小节，用下面模板实时更新；当前 Slice 完成后保留最终结论和代表图路径，删除中间轮次路径。这样既不会用大切片掩盖单项，也不会长期堆积 66 项 × 多轮截图日志。

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 示例 | 未开始 | — | — | — | — | — |

表中任一项目未到“用户确认”，或未经用户明确同意延期，其所属 Slice 都不能标记完成。自动测试通过只能填写“鲁棒性”的一部分，不能代填两轮 UI 校对或用户确认。

#### A. 页签、顶栏和全局显示

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| SH-01 | workspace 书页签 | 打开书、聚焦既有书、关闭、邻居回退、刷新恢复；退役内部重复页签 | 快速切书、迟到路由、损坏持久化 | V0/V1 |
| SH-02 | Authoring 工具条 | 单行分组、横向访问、更多菜单、与页签紧密连接 | 工具过多、390、200% zoom、HMR 样式缺失 | V1/V7 |
| TB-01 | 撤销/重做 | enabled/disabled、快捷键、点击反馈、焦点归还 | AI 事务与人工历史隔离、快速重复点击 | V1/V3 |
| TB-02 | 粗体/斜体 | active/混合选区/disabled、中文选区表现 | 跨 mark 查找、撤销、IME 选区 | V1/V3 |
| TB-03 | 分隔线 | 入口、插入位置、上下段距、撤销 | 空段、unit 边界、连续插入 | V1/V3 |
| TB-04 | 排版 | 字体、字号、行距、首行、段距的面板编排与即时预览 | 极值、刷新持久、弹层碰撞、窄屏 | V1/V7 |
| TB-05 | 取名 | 入口、生成条件、候选、换一批、插入/复制 | 空结果、重复名字、弹层越界 | V1/V6 |
| TB-06 | 打字机 | active 状态、光标行位置、退出后恢复 | 长文、zoom、批注/推演打开时 | V1/V7 |
| TB-07 | 聚焦 | 当前段与非当前段层级、切段过渡 | Ghost/选区/设定高亮并存 | V1/V3 |
| TB-08 | 专注/全屏 | 进入、退出、Esc、退出按钮和布局恢复 | inspector 打开、sheet 打开、刷新恢复 | V1/V7 |
| TB-09 | 查找替换 | 查找、上下一个、替换、全部替换、关闭 | 跨 mark、长文、零结果、正文 revision | V1/V3 |
| TB-10 | 更多 | 素材、导出、回到冒险等低频动作的分组和导航 | disabled、下载失败、重复打开 | V1/V6 |
| ST-01 | 保存反馈 | 保存中、已保存短显、失败、重试；底栏协调 | quota、切章前保存失败、连续输入 | V1/V8 |

#### B. 左导航、文档和当前场简报

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| NV-01 | 搜索章节/探索 | 输入、清空、零结果、命中高亮 | 50+ 文档、中文组合输入、切书清空 | V2 |
| NV-02 | 新建章/新建书 | 入口层级、默认命名、创建后焦点和选中 | 保存失败、快速双击、空书 | V2 |
| NV-03 | 快速落笔 | 新建未编排 exploration、标题、返回正文 | 重复触发、刷新恢复、迁移旧速记 | V2 |
| NV-04 | 构思/正文分组 | 展开、层级、空态、探索映射大纲 | 30 章 + 20 探索、切换不跳版心 | V2 |
| NV-05 | 章节行 | `第一章　章名`、字数、active、hover、focus | 长章名、零字章、重命名 | V2 |
| NV-06 | 排序/删除 | 拖动把手、落点、确认、邻居回退 | 保存失败回滚、当前章删除、快速切换 | V2/V8 |
| NV-07 | 世界书绑定 | 当前绑定、换绑、取消、缺失来源 | 切书、加载中、失效世界书、保存失败 | V2/V5 |
| SC-01 | 当前场简报 | 时间/人物/地点/事件四轴、空态、冲突、未读 | 长名称、轴缺失、切 caret/章 | V2/V5 |
| SC-02 | 当前场入口 | 点击轴打开正确右详情，空轴进入选择 | inspector 已开、重复点击、1024/390 | V2/V5/V7 |

#### C. 稿面、writingUnit 和基础编辑

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| ED-01 | 章节/探索标题 | 稿面内标题、中文章序、编辑、保存反馈 | 长标题、空标题、切文档、保存失败 | V1 |
| ED-02 | 中文输入 | 正文、换段、删除、粘贴、composition | Windows IME 30 分钟、emoji、换行文本 | V1/V3/V8 |
| ED-03 | 首行与段落排版 | 缩进、段距、引文、列表、分隔线 | 字体切换、zoom、选区格式 | V1 |
| ED-04 | writingUnit 常态 | 多段单元、页边刻度、当前态、hover | 长单元、空单元、新生成多段 | V3 |
| ED-05 | 单元拆分/合并/移动 | 入口、预览、落点、撤销 | 批注迁移、来源保留、首尾单元 | V3 |
| ED-06 | 选区浮条 | 批注、素材/记住等真实动作、位置和关闭 | 反向选区、跨段、滚动、视口边缘 | V3/V6 |
| ED-07 | 空行命令菜单 | 一级、二级、键盘、鼠标、返回、关闭 | IME、Space/`/`、边缘翻转、窄屏 | V3 |
| ED-08 | 当前行/段落聚焦 | 光标行、当前段、unit 的强调层级 | Ghost、批注、设定词同时出现 | V3 |
| ED-09 | 正文滚动与 caret | 唯一滚动 owner、切文档恢复、打字机 | 第 80 段、resize、zoom、字体改变 | V1/V7 |
| ED-10 | 稿面底栏 | 字数、字符、修订、短暂保存状态 | 长数字、390、安全区、错误态 | V1/V7 |

#### D. Ghost、推演、改写和 AI 可见状态

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| AI-01 | 输入停顿 Ghost | armed/requesting/review/empty/error/dismissed | 重复 revision、继续输入、空返回 | V3 |
| AI-02 | 光标停驻 Ghost | 移动取消、稳定触发、同 fingerprint 一次 | 浏览前文、上下键、滚动、切章 | V3 |
| AI-03 | Ghost 采纳 | Tab 全量、快捷键渐进、点击、Esc 丢弃 | 撤销、格式、caret 变化、迟到结果 | V3 |
| AI-04 | 推演入口/composer | 单元下入口、指令、必要选项、停止 | anchor 丢失、重复打开、切文档 | V4 |
| AI-05 | 推演生成态 | 占位高度、进度、停止、慢请求 | provider 超时、取消竞态、页面滚动 | V4 |
| AI-06 | 可编辑推演草稿 | 编辑、草稿撤销、恢复原稿、段落排版 | 长草稿、IME、stale、保存失败 | V4 |
| AI-07 | 推演采用/丢弃 | 采用编辑稿、一次写入、焦点恢复、撤销 | scene/outline delta、重复点击、刷新 | V4/V5 |
| AI-08 | 按批注改写 | 指令、多个候选、diff、采用、忽略 | 跨 unit、stale、请求失败、切章 | V6.1 |
| AI-09 | 章节审查 | 触发、生成、结果、定位、关闭 | 长章节、空返回、旧结果作用域 | V6.1/V6.6 |
| AI-10 | 被动通知 | routine 静默、冲突聚合、可达查看 | 重复候选、切书、频率预算 | V6.6/V8 |

#### E. 当前场—世界书—正文闭环

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| SW-01 | 现场人物列表 | 已在场、视角、行动者、对象、空态 | 同名人物、失效 ref、长名称 | V5/V6.4 |
| SW-02 | 从世界书添加人物 | 搜索、选择、摘要、取消、打开完整设定 | 未绑定、加载失败、切书、重复添加 | V5 |
| SW-03 | 地点/时间/事件编辑 | 选择、纠正、安排、证据 | 缺失条目、冲突、继承前文 | V5/V6.4 |
| SW-04 | 作用范围 | 纠正当前/安排下一段/仅本次的解释和选中态 | 返回、重复切换、错误默认值 | V5 |
| SW-05 | 正文设定词匹配 | 轻标注、点击详情、范围说明 | caret 前范围、后文排除、同义词 | V5/V6.3 |
| SW-06 | 以此推演 | 人物/地点/事件进入 composer，指令可见 | stale source、取消、重新生成 | V5 |
| SW-07 | 采纳后兑现 | 正文、scene delta、大纲回执和简报更新 | 保存失败、observer 失败、撤销 | V5 |

#### F. 右侧 rail 与六个详情工具

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| RT-01 | rail 本体 | 图标顺序、分组、active、tooltip、badge | 高度不足、390 横向滚动、切工具 | V6/V7 |
| RT-02 | inspector 壳 | 标题、关闭、返回、锁定/跟随、双栏动作 | 1024 overlay、390 sheet、焦点恢复 | V6/V7 |
| RT-03 | 批注 | 空态、锚定列表、编辑、改写、删除 | 重叠避让、滚动、orphan、跨 unit | V6.1 |
| RT-04 | 大纲 | 当前邻域、列表、详情、映射、编辑 | 空大纲、长树、切章、保存失败 | V6.2 |
| RT-05 | 设定 | 当前命中、分组、详情、证据、完整页跳转 | 零命中、加载、失效 ref、dual | V6.3 |
| RT-06 | 现场 | 四轴概览、单条详情、纠正、安排、推演 | 继承、冲突、未绑定、旧作用域 | V6.4 |
| RT-07 | 历史 | 当前修订、恢复稿、快照、片段历史 | quota、恢复失败、删除、探索作用域 | V6.5 |
| RT-08 | 上下文 | 实际 included/excluded、章节参与、receipt | 无调用、未来章、截断、stale | V6.6 |
| RT-09 | 角色/素材退场 | 角色并入现场、素材进入更多/独立 tab | 旧深链、已有打开 tab、无数据丢失 | V6/V8 |

#### G. 响应式、可访问性和长期稳定性

| ID | 功能 | 必须逐一实现和校对的范围 | 重点鲁棒性 | Slice |
| --- | --- | --- | --- | --- |
| RS-01 | 1440+ 桌面 | 四列关系、正文宽度、固定侧栏 | inspector 开关、长列表、双栏 | V1–V7 |
| RS-02 | 1024 笔记本 | 左栏压缩、inspector overlay、正文稳定 | zoom、长标题、打开多个工具 | V7 |
| RS-03 | 390 手机 | 正文默认、互斥 sheet、底部 rail | 安全区、键盘、返回、滚动锁 | V7 |
| RS-04 | 200% zoom | 工具可达、可读重排、无裁切 | 有效 195px、焦点顺序、菜单 | V7 |
| RS-05 | 键盘与焦点 | Tab 顺序、focus-visible、Escape 一层关闭 | modal/sheet/menu/Ghost 竞争 | V3/V6/V7 |
| RS-06 | 样式层失效 | 最终 Authoring 骨架仍保持结构 | HMR、构建顺序、缺 scoped 层 | V0/V7/V8 |
| RS-07 | 作用域与竞态 | 切书/章/探索后无旧 UI 和迟到写入 | 快速导航、慢请求、保存窗口 | 全阶段 |
| RS-08 | 存储与恢复 | quota、损坏 payload、恢复草稿、刷新 | 一次性提示、零数据覆盖 | V2/V6.5/V8 |

### 6.4 当前验收记录

V0/V1 首批已于 2026-08-29 在真实页面（分支 `integration/consolidation-20260823` 工作区 + 本地 fixture）逐项实施与校对。执行环境与产物：

- fixture：`scripts/authoring-ui/rollout-fixture.mjs`（走正式 repository/store/schema 装载《雾港纪事》：5 章、第五章 3,577 字、3 个探索文档、世界书 4 条目、2 条批注、1 个大纲节点、3 份快照、当前场锚点；localStorage 快照落盘 `tmp/authoring-rollout/fixture-localstorage.json`）；
- 逐项验收脚本：`scripts/authoring-ui/rollout-acceptance.mjs`（真实点击/输入驱动，19/19 通过，报告 `tmp/authoring-rollout/acceptance/report.json`）；
- 几何报告：`tmp/authoring-rollout/geometry-report.json`（`.wall__dossier-scroll` 为唯一正文滚动 owner，1440/1024/390 一致）；
- selector ownership 清单：`tmp/authoring-rollout/selector-ownership.md`（778 个唯一 selector，71 个跨文件/多层重复）；
- 代表图：`tmp/authoring-rollout/baseline/baseline-{top,mid}-{1440,1024,390}.png`、过程证据 `tmp/authoring-rollout/acceptance/*.png`。

V1 批次已经用户 2026-08-29 截图验收（含两轮返工：工具栏精简/更多菜单 fixed/专注全屏/查找条重做/弹层缩放对位/选区浮条/右 rail 文字标签），并确认中央稿面上下灰带修复后进入 V2。

V2 批次（左导航与当前场）2026-08-29 实施，Gate 脚本 `scripts/authoring-ui/rollout-v2-check.mjs` 5/5 通过（密集 30 章 + 20 探索经真实 UI 路径构造；报告 `tmp/authoring-rollout/v2-check-report.json`；代表图 `tmp/authoring-rollout/v2/`）。台账如下，待用户确认。

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| NV-01 | UI 第二次校对 | 通过 | 部分（30 章过滤/清空/零结果通过；中文组合输入留 V8） | 通过 | 待确认 | 搜索按“序号+标题”过滤章节；零结果有空态 |
| NV-02 | UI 第二次校对 | 通过 | 部分（新建章循环 25 次全部持久化；保存失败回滚路径留 V8） | 通过 | 待确认 | 新建章后焦点与选中跟随 |
| NV-03 | UI 第二次校对 | 通过 | 通过（快速落笔默认名改“速记 M月D日 HH:mm”，重复创建/返回正文通过） | 通过 | 待确认 | 探索创建即进入编辑，返回正文锚点保留；**用户返工**：构思头删去“未归章 · 不进入正文”说明字，返回正文按钮单行右置 |
| NV-04 | UI 第二次校对 | 通过 | 通过（30 章 + 20 探索全单行渲染，行高 24px 无卡片墙） | 通过 | 待确认 | v2/v2-dense-nav-1440.png |
| NV-05 | UI 第二次校对 | 通过 | 通过（长章名/零字章由空标题态覆盖） | 通过 | 待确认 | 章序/章名分体 span，固定 10px 间隔；当前章 2px 左刻度 + 浅底 |
| NV-06 | 主路径可操作 | — | 部分（HTML5 拖拽既有实现，把手 hover 显隐未重做，留 V2 后续） | — | 待确认 | 拖拽排序保持既有事务与回滚 |
| NV-07 | UI 第二次校对 | 通过 | 部分（绑定行存在；失效世界书/切书留 V5） | 通过 | 待确认 | 世界书绑定行保持 |
| SC-01 | UI 第二次校对 | 通过 | 通过（当前场实底背景，列表滚动不透行） | 通过 | 待确认 | v2/v2-scene-rail.png；一行一轴简报常驻左下 |
| SC-02 | UI 第二次校对 | 通过 | 部分（轴点击开右详情/空轴进编辑链路确认；1024/390 sheet 留 V7） | 通过 | 待确认 | open-detail→openSceneDetail 接线确认 |

V2 批次已经用户"继续推进"指令放行（2026-08-29），进入 V3。

**Slice V3（writingUnit、短 Ghost 与空行命令）2026-08-29 实施**：单元可见语法（hover 页边刻度、当前行 overlay、Ghost 出现时强调预算降级）在编辑器活层样式内已存在并逐项核对；本轮修复三个真实缺陷并更新 journey harness：
1. **推演入口标签僵死**：BlockGap widget 的 key 不含"锚定单元是否有正文"，ProseMirror 对同 key widget 复用旧 DOM，空章开场后输入文字标签仍停留"推演本章开场"（此前被每次输入触发的整文档 setContent 掩盖，撤销修复后暴露）——key 加入 text/empty 状态。
2. **同内容守卫 refined**：守卫只跳过 setContent 重放，不再替换 currentDocument/storage 书账——书账替换会让采用事务的候选校验拿到另一份引用而误判 stale（A/B 实测确认）。
3. **journey harness 同步**：标题 aria-label 恢复常量"章节标题"（契约选择器）；J14 建书改走左栏"新建书"（单书时页内页签退役）；J2/J12 按"速记"新默认名定位。
4. 稿面标题章序在作者标题自带"第X章"时不再重复显示。

Focused Gate（BASE=5173，`node scripts/authoring-journeys-smoke.mjs`）：**J6 Ghost 联想、J8 空行命令/Ghost 隔离、J9 IME 组合态、J2/J3/J4/J5、J11 长文、J12 探索批注隔离、J13 快照、J14 双书隔离 全部通过**；V1 acceptance 19/19、V2 Gate 5/5、UI contract 6/6 回归通过。

**遗留阻断缺陷已专项修复（2026-08-29 同日）**：
- **J10 采纳链 target-unit-stale（已修复）**：根因是 PM 文档节点的 `unitRevision` attrs 只在节点创建时写入，页面侧 revision 演进从不回写 PM attrs——旧 setContent 风暴每次整树替换 PM 文档、掩盖了失步；同内容守卫移除风暴后，insertAsNewWritingUnit 读 PM attrs（恒 0）与 composer 的 expectedUnitRevision（26）必然失配。修复：stale 校验改读 canonical 文档（currentDocument）中同 unitId 的修订。专项后全组 journey **passed**（J1–J14 全绿，仅 J7 预期 abort 诊断）。
- **J1 右键菜单焦点恢复（已随上项复绿）**：同一失步在 J1 断言上的投射，canonical 修复后 Esc/键盘两条断言通过。
- 附带发现（记录不阻塞）：J13 快照恢复路径经 `createWritingDocument(fallbackMarkdown)` 重建文档，单元修订清零（[ab-apply] firstUnitRev 0 / prev 24）——恢复语义本身通过，修订保真问题留 V6.5 历史切片评估。

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| ED-04 | UI 第二次校对 | 通过 | 通过（单元刻度/当前行/常态无卡片；多段单元单一来源由 schema 保证） | 通过 | 待确认 | 编辑器活层样式核对；J11 长文通过 |
| ED-05 | UI 第二次校对 | 通过 | 通过（J10 修复后单元写入链路全通；拆分/合并保持既有事务） | 通过 | 待确认 | J10 ✓ |
| ED-06 | UI 第二次校对 | 通过 | 通过（反向选区/滚动/视口钳制实测） | 通过 | 待确认 | tb02-selection-bar.png |
| ED-07 | UI 第二次校对 | 通过 | 通过（J8 空行命令菜单/Ghost 隔离通过） | 通过 | 待确认 | J8 ✓ |
| ED-08 | UI 第二次校对 | 通过 | 通过（Ghost 出现时当前行/unit 降级规则在 block-native 生效） | 通过 | 待确认 | tb07-focus.png + 强调预算规则 |
| ED-09 | UI 第二次校对 | 通过 | 通过 | 通过 | 已确认（2026-08-29） | 108/390 滚动 owner 保持 |
| AI-01 | UI 第二次校对 | 通过 | 通过（J6 重复 revision/继续输入/空返回场景通过） | 通过 | 待确认 | J6 ✓ |
| AI-02 | UI 第二次校对 | 通过 | 通过（cursor-dwell 指纹、浏览前文不闪） | 通过 | 待确认 | J6/J9 ✓ |
| AI-03 | UI 第二次校对 | 通过 | 通过（J6 Tab/渐进采纳通过；J10 修复后采纳写入链路全通） | 通过 | 待确认 | J6 ✓ J10 ✓ |

**Slice V4（长推演临时写作层）2026-08-29 审计与收口**：用确定性 provider mock 驱动五状态实拍（`scripts/authoring-ui/rollout-v4-audit.mjs`，图 `tmp/authoring-rollout/v4/`）。现状已高度符合计划（底座来自可靠性计划 Slice 2/4）：composer 首态紧凑（181px、指令一行、类型四选、高级设置折叠）；review 与正文同版心（896px=896px）同字体同字号（17px）同行高（30.6px），来源线为头部一行说明 + 24% 透明底，非大面积蓝卡；生成前后段距一致（17.85px，scrollHeight 840→840）；采纳后焦点落新正文末尾、编辑稿（非原模型稿）写入、草稿层完全消失；stale/error 态实图采用 J10 修复前真实失败截图（草稿保留 + 告警贴近动作区）。无代码修改需求，纯审计收口。

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| AI-04 | UI 第二次校对 | 通过 | 通过（入口在目标单元间隙，统一“推演”命名；重复打开/切文档由 J10 交互 owner 覆盖） | 通过 | 待确认（1440 主图） | v4/v4-1-composer-initial.png；高级设置折叠不铺表单 |
| AI-05 | UI 第二次校对 | 通过 | 通过（生成态占位：前后 scrollHeight 一致；取消/慢请求由 provider mock 场景覆盖） | 通过 | 待确认（1440 主图） | v4/v4-2-generating.png（mock 完成快，抓到完成瞬间） |
| AI-06 | UI 第二次校对 | 通过 | 通过（编辑/恢复生成稿/已修改态实拍；草稿编辑不触发正文 Ghost/autosave 由 J10 断言保证） | 通过 | 待确认（1440 主图） | v4/v4-3-review-editable.png、v4-4-review-edited.png；同版心同排版 |
| AI-07 | UI 第二次校对 | 通过 | 通过（采纳编辑稿写入、焦点回归、草稿层消失、段距复原；J10 刷新持久断言） | 通过 | 待确认（1440 主图） | v4/v4-5-adopted.png；stale/error 实图 = J10 修复前失败截图（tmp/authoring-journeys/20260829T140158Z_*/j10/） |

**Slice V5 首版（当前场→世界书→推演→正文闭环）2026-08-29/30 实施收口，临时意图所有权已由 C1-2 替换**。首版曾以 `sceneAnchors.plannedCharacterIds` 保存“安排下一段入场”；该字段只保留旧数据兼容，不再是新运行的临时意图真源。C1-2 起，纠正当前才写 author anchor，安排下一段与仅供本次均按目标留在内存 session，见下方 C1-2 记录。首版同时修复：
1. **投影丢失"沿用前文锚点"**：`buildAuthoringSceneProjection` 调用从未传 `unitOrder`，继承解析只能在当前单元找锚点——修后旧锚点正确向前继承；
2. **失焦清空当前场**：`activeWritingUnitId` 为空（点工具/失焦）时投影整体退化为待设置——回退到文档末单元（当前场是章节事实，不随焦点消失）；
3. **场景流内核 payload 超限**：`narrativeKernelPayload` 是单个 text part（合同上限 8000 字符），携带 worldbook/scene 块后越限——按块 2400 字符界额截断（保持可解析，仅影响 payload 内参考副本）。另修 fixture 锚点 status 字段误用（该字段是失效标记，非空即 stale）。
4. **J1 右键菜单焦点恢复（V3 遗留缺陷根因修复）**：`restoreSelectionBookmark` 用 tiptap `commands.focus()`（异步 focus 管理），右键菜单刚卸载、焦点尚在菜单按钮时 DOM 选区滞留不画——改用 PM 原生 `view.focus()` 同步回写 DOM selection；矩阵两轮 passed 确认稳定。

当前 Gate：`scripts/authoring-ui/rollout-v5-check.mjs` 5/5——完整验收故事（当前场补艾德加→安排下一段→确认采纳前零现场写入→编辑 Ghost→采用→当前场更新“莉娜、艾德加”）全链通过，并把 console/page error 纳入第五项硬断言。Focused C1-2 测试 3 文件 / 59 用例通过；代表图仍由 `tmp/authoring-rollout/v5/` 临时生成。

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| SW-01 | UI 第二次校对 | 通过 | 通过（同名人物/失效 ref 由候选置顶与 missing 提示覆盖） | 通过 | 待确认（1440 全链图） | v5-4-adopted-rail.png；在场/视角/待入场分层 |
| SW-02 | UI 第二次校对 | 通过 | 通过（未绑定/搜索/重复安排由 curation 守卫覆盖） | 通过 | 待确认（1440 全链图） | v5-1-curation.png、v5-2-planned.png；艾德加安排下一段 |
| SW-03 | UI 第二次校对 | 通过 | 部分（地点/时间编辑在调整表单可用；冲突继承细节留 V6.4） | 通过 | 待确认 | 表单三段式 时间→地点→人物 |
| SW-04 | UI 第二次校对 | 通过 | 通过（三档范围语义齐备：纠正当前显式保存；安排下一段/仅供本次按目标留在内存，不提前写 scene anchor） | 通过 | 待确认 | v5-1-curation.png；人物/地点使用同一行式动作语言 |
| SW-05 | 主路径可操作 | — | 部分（设定词轻标注为既有 worldbook mentions 装饰；范围说明/点击详情留 V6.3） | — | 待确认 | 留 V6.3 补验 |
| SW-06 | UI 第二次校对 | 通过 | 通过（临时动作打开绑定目标 writingUnit 的 composer；provider/Ghost 编辑/保存失败期间保留，放弃与作用域切换清除） | 通过 | 待确认（1440 全链图） | v5-2-planned.png；composer 显示人物及作用范围 |
| SW-07 | UI 第二次校对 | 通过 | 通过（采纳只兑现 frozen next-passage effect；run-only 零 scene delta；保存失败原地重试且不重新请求） | 通过 | 待确认（1440 全链图） | v5-4-adopted-rail.png；rail 人物轴更新为“莉娜、艾德加” |
| NV-06a | UI 第二次校对 | 通过 | 通过（右键打开/动作/关闭、删除带确认与回滚；Escape/点击外部关闭） | 通过 | 待确认 | **用户返工（桌面右键预案）**：章节行右键＝打开/重命名/导出本章/历史版本/删除本章（danger+确认）；卷组右键＝新建章节/导出整本书；`@contextmenu.prevent` 统一自研菜单（打包后无浏览器原生右键）；正文 `.context-menu` 样式从死层救活到 block-native；证据 v2/v2-chapter-context-menu.png、v2-volume-context-menu.png、v2-manuscript-context-menu.png |

| ID | 当前状态 | UI 校对 1 | 鲁棒性 | UI 校对 2 | 用户确认 | 最新代表图/结论 |
| --- | --- | --- | --- | --- | --- | --- |
| SH-01 | UI 第二次校对 | 通过 | 部分（切章/刷新恢复通过；迟到路由留 V7） | 通过 | 已确认（2026-08-29） | 单书时内部书页签退役；`?bookId` 刷新恢复；baseline-top-1440 |
| SH-02 | UI 第二次校对（第三轮返工后） | 通过 | 部分（390 单行横向滚动通过；200% zoom 留 V7） | 通过 | 已确认（2026-08-29） | **用户返工**：工具条只留实用动作 `撤销/重做·排版/取名·专注·查找·更多`；B/I/分隔线移入选区浮条，打字机/聚焦移入排版面板；“更多”原为 absolute 菜单被工具条裁切成死按钮，已改 fixed 弹层（Teleport body）；第三轮修 zoom 错位（writingUiScale + 右缘对齐按钮）；acceptance/tb10-more-menu.png |
| TB-01 | UI 第二次校对 | 通过 | 部分（发现并修复 P0：文档同步环 setContent 打断撤销栈、光标摔至文末；同内容守卫修复后撤销/重做/焦点回归通过） | 通过 | 已确认（2026-08-29） | 编辑器 document watcher 增加同内容守卫；Ctrl+Z / Ctrl+Shift+Z / 焦点归还验证通过 |
| TB-02 | UI 第二次校对（第三轮返工后） | 通过 | 部分（加粗/取消/字重同步通过；IME 选区留 V8 外部门禁） | 通过 | 已确认（2026-08-29） | **用户返工**：B/I 从工具条移入选中文字浮条；Ctrl+B 加粗/取消、选区字重 ≥600 验证通过 |
| TB-03 | UI 第二次校对 | 通过 | 通过（插入/撤销原子；unit 边界连续插入留 V3） | 通过 | 已确认（2026-08-29） | 分隔线入口移入选区浮条；插入/撤销原子保持 |
| TB-04 | UI 第二次校对（第三轮返工后） | 通过 | 部分（即时预览/持久/极值通过；弹层碰撞修复：fixed 定位 + zoom 缩放换算；窄屏留 V7） | 通过 | 已确认（2026-08-29） | **用户返工**：打字机/聚焦开关并入排版面板；面板 fixed 完整弹出（acceptance/tb04-font-panel.png） |
| TB-05 | UI 第二次校对 | 通过 | 部分（候选/换一批/关闭通过；弹层越界已由 fixed 模态覆盖；空结果留 V6） | 通过 | 已确认（2026-08-29） | acceptance/tb05-name-gen.png；候选 ≥8、可换一批、× 关闭 |
| TB-06 | UI 第二次校对（用户返工后） | 通过 | 部分（开关/滚动生效；zoom 留 V7） | 通过 | 已确认（2026-08-29） | **用户返工**：打字机移入排版面板开关行；开启后滚动主动调整，退出恢复 |
| TB-07 | UI 第二次校对（用户返工后） | 通过 | 通过（此前为死功能：三份 CSS 均无 is-focus-paragraph 规则，已补齐） | 通过 | 已确认（2026-08-29） | **用户返工**：聚焦移入排版面板开关行；非当前段 opacity 0.38、当前段 1，退出恢复全量 |
| TB-08 | UI 第二次校对（用户返工后） | 通过 | 部分（进入/Esc 退出/布局恢复通过；inspector 并存留 V6） | 通过 | 已确认（2026-08-29） | **用户返工**：专注此前只隐藏 AppShell 壳、未隐藏 Authoring 顶栏/左栏/rail/工作台页签，已补齐为真全屏（正文单列占满 + 退出按钮），acceptance/tb08-zen.png |
| TB-09 | UI 第二次校对（用户返工后） | 通过 | 通过（修复：换查找词不重搜导致零结果不可达、替换作用旧结果；watch(findText) 修复后计数/零结果/单处替换/关闭全通过） | 通过 | 已确认（2026-08-29） | **用户返工**：查找条 UI 重做（旧样式全部在死层裸奔）——圆角浮条：搜索图标+输入+计数+↑↓ ｜ 替换输入+单处/全部 ｜ × 关闭，acceptance/tb09-find-bar.png |
| TB-10 | UI 第二次校对（用户返工后） | 通过 | 部分（菜单项/disabled 通过；素材入口退役后导出路径保持） | 通过 | 已确认（2026-08-29） | **用户返工**：菜单从 `<details>`+absolute（被工具条裁切成死按钮）改为按钮 + fixed 弹层；含收件箱/素材库/自动联想/导出/返回，Esc 与点击外部关闭 |

第二轮返工记录（2026-08-29，用户截图对照作家助手）：① 工具条只留实用动作；② “更多”死按钮根因是 absolute 菜单被 42px 单行工具条 `overflow` 裁切（与排版面板同型缺陷），改 fixed 弹层；③ 专注缺真全屏（Zen 只隐藏 AppShell 壳），补齐 cork/shelf/rail/工作台页签隐藏 + main 单列；④ 查找替换条旧样式全部位于死层，重新设计。返工后验收 19/19 重新通过。

第三轮返工记录（2026-08-29，用户实测反馈）：① “更多”菜单展开错位——根因是应用级 UI zoom（设置页缩放）下 CSS px ≠ 视觉 px，弹层定位未除以缩放；统一 `writingUiScale()` 换算并改为右缘对齐触发按钮（排版面板同修）。② 选区浮条白框覆盖不到位——浮条容器写死 `width: 166px` 而按钮集已扩为 B/I/分隔线/批注/素材/事实，改 `width: max-content` + 渲染后实测 offsetWidth 交钳制器处理。③ 右 rail 对照“校对/拼字/大纲/角色/设定/灵感/双栏/妙笔”补实用化：图标下加文字标签（批注/大纲/角色/设定/现场/双栏/AI/历史），素材移出 rail（已在“更多”），rail 44→52px（含 inspector 偏移与 UI contract 同步更新）。返工后验收 19/19、UI contract 6/6 再次通过。
| ST-01 | UI 第二次校对 | 通过 | 通过（保存中/已保存短显/底栏协调） | 通过 | 已确认（2026-08-29） | acceptance/st01-save-chip.png；chip 短显后消失 |
| ED-01 | UI 第二次校对 | 通过 | 通过（长标题/编辑/保存反馈；切文档恢复） | 通过 | 已确认（2026-08-29） | 稿面内标题显示 `第五章　章名`，章序与左导航同源；无输入框边框 |
| ED-02 | UI 第二次校对 | 通过 | 部分（中文输入/换段/撤销通过；Windows IME 30 分钟耐久为外部门禁） | 通过 | 已确认（2026-08-29） | 输入、Enter 换段、Ctrl+Z 撤销均通过 |
| ED-03 | UI 第二次校对 | 通过 | 通过（默认排版改为中文优先栈 + 17px/1.8/首行两字缩进） | 通过 | 已确认（2026-08-29） | 计算样式：Microsoft YaHei 17px/30.6px/34px 缩进 |
| ED-09 | UI 第二次校对 | 通过 | 通过（唯一滚动 owner、切文档恢复） | 通过 | 已确认（2026-08-29） | 正文滚动时 cork/shelf/footer rect 零位移；页面本身不滚 |
| ED-10 | UI 第二次校对 | 通过 | 通过（长数字、390 底栏避让底部 rail） | 通过 | 已确认（2026-08-29） | 底栏仅字数/字符/修订；自动联想开关移入更多菜单 |
| RS-01 | UI 第二次校对 | 通过 | 部分（1440 结构通过；1024/390 重排由 V7 统一收口） | 通过 | 已确认（2026-08-29） | 左栏/稿面/rail 同行、无横向滚动 |
| RS-06 | UI 第二次校对 | 通过 | 重要发现 | 通过 | 已确认（2026-08-29） | **Writing.scoped.css 为死层**：编译产物 scope id 为空（`.font-panel[data-v-]`），3145 行规则从未命中；弹层定位/聚焦样式等已收归 block-native/global；HMR/构建顺序边界留 V7/V8 |
| RS-07 | UI 第二次校对 | 通过 | 通过（5 章快速连切无旧作用域、无重复工具栏、0 console 错误） | 通过 | 已确认（2026-08-29） | 快速切章后章序/内容/工具栏一致 |

## 7. 实施切片

### Slice V0：基线锁定与页面可观测性

**作者任务：** 打开一本已有长章节，不进行操作，先确认页面没有结构崩塌。

#### 工作

1. 固定上述 fixture 和五个可见状态，不新建业务 mock store；只通过正式 repository/localStorage 接口装载。
2. 记录 1440/1024/390 下顶栏、左栏、正文、rail、inspector 的 bounding boxes、滚动 owner 和计算样式。
3. 建立 selector ownership 清单，标出三份 Authoring CSS 中重复的结构 selector、断点和最终优先级。
4. 在视觉实现前保存一组基线图；不把当前图称为通过图。
5. 确认运行服务实际 branch、CSS 产物和 HMR 状态，避免改了工作区却验收另一份构建。

#### Gate

- fixture 不是空态；截图能看到长正文、当前场和工具 rail；
- 报告能指出唯一正文滚动 owner；
- SH-01、RS-06、RS-07 建立第一版逐功能状态和证据记录；
- 本切片不改视觉、不跑全量测试、不创建提交。

### Slice V1：中央稿面与顶栏——先让“写字”本身成立

**作者任务：** 从页签进入第五章，阅读几段并继续输入，不打开任何右侧详情。

这是第一张需要用户确认的代表界面；未确认前暂停 V2–V8。

#### 可见范围

- AppShell 页签与 Authoring 工具条的上下关系；
- 中央背景、稿面、章节标题、正文排版、底栏；
- 左栏和 rail 只保留占位尺寸，不在本切片重设计内容。

#### 工作

1. 以 AppShell workspace tab 作为书/完整功能页的唯一页签 owner，设计内部书页签的退役步骤；接线未验证前先视觉降重，不直接删路由同步。
2. 顶栏收成稳定单行，确定核心工具顺序和“更多”归属；移走常驻素材入口和重复导航。
3. 固定中央连续工作面，不使用完整卡片边框；建立左右白、中央浅蓝灰、稿面轻微抬升的低色差关系。
4. 把标题放回稿面内部，使用 `第一章　章名`；去掉原生输入边框、`01`、多余 kicker 和假状态。
5. 统一字体、字号、行高、首行缩进、段距、引号、selection、placeholder 和正文列宽。
6. 底栏改为安静边缘信息；保存成功只短暂出现，失败保持可见且可定位原因。
7. `Authoring.block-native.css` 只拥有页面骨架、尺寸和主题 token；稿面内部规则逐步回到 Notebook/相关组件，不在三层 CSS 继续加第四层覆盖。

#### 明确不做

- 不在这一切片改 Ghost 算法、当前场数据、大纲或记忆；
- 不顺手重画左栏和所有右面板；
- 不把整个 `Authoring.vue` 拆组件；
- 不为每个字号和间距单独跑测试。

#### Gate

- 1440 主图第一眼是标题和正文；工具条与页签紧密但不拥挤；
- 5000 字滚动时标题、正文和底栏没有第二滚动条；
- 标题在稿面内，章节显示朴素，首行缩进与中文引号正确；
- 1024 正文仍有可读宽度；390 默认只见正文，不串出左栏；
- SH/TB/ST、ED-01/02/03/09/10 逐项完成两轮 UI 校对和适用鲁棒性状态；
- 用户明确确认方向后才进入 V2。

### Slice V2：左侧“构思—正文—当前场”连续导航

**作者任务：** 在同一本书中从速记进入探索，再回到第五章，并查看当前场简报。

#### 信息架构

```text
构思
  ＋ 快速落笔
  未编排探索
  已关联大纲的探索（缩进在对应节点下）
正文
  第一卷                         5章
    第一章　魔力异常             2652
    第二章　……                    680
当前场
  时间   入夜前
  人物   莉娜
  地点   旧港税务所
  事件   石柱回应
```

#### 工作

1. 左栏只显示当前书的文档结构；书本身由 workspace tab 表达，不再占一张大卡或重复标题。
2. 构思和正文使用连续树，不用每项卡片；`快速落笔` 复用已有 exploration repository。
3. 正文章节显示中文章序与章名，章序和名称之间使用稳定间隔；当前章用单一左刻度和浅底表达。
4. 拖拽排序只在 hover/拖动时出现把手和落点；平时不让控制图标挤占标题。
5. 当前场固定在左栏下部；章节多时只滚动上半区，当前场不被推离视口。
6. 当前场常态只显示一行一轴的简报；“当前落笔处”等重复状态改为辅助可访问文本，只有“沿用前文/待设置/冲突”才显式提示。
7. 点击当前场任一轴在右侧打开对应详情；点击空人物/地点直接进入选择流程，而不是打开空详情。

#### Gate

- 30 章 + 20 探索仍可扫描，不出现卡片墙；
- 第一章有正确缩进，章序与章名有间隔；
- 构思/正文切换稿面宽度、标题基线和 scrollTop 不跳；
- 当前场始终可见但不抢正文；
- NV-01 至 NV-07、SC-01/02 必须逐项截图、操作、校对并更新状态；
- 1440 左栏主图经用户确认后再扩散到 1024/390 sheet。

### Slice V3：writingUnit、短 Ghost 与空行命令

**作者任务：** 在含三段文字的单元中移动光标、输入一句、等待短补全、部分采纳；再到空行按 `/` 打开命令。

#### 工作

1. 冻结 writingUnit 的可见语法：常态开放排版，hover 页边刻度，当前 unit 低强度信号，拖动时才显示整体范围。
2. 确保一个含数段正文的单元保持一个来源/移动对象；不按自然段重新切块。
3. 短 Ghost 直接跟随 caret 后方排版，颜色弱于正文，字体、行高、缩进与当前段一致；不添加卡片、标题或常驻“正在续写”。
4. 请求超过反馈阈值时只在 caret 附近显示细小生成信号；空返回、取消和被动失败不留下错误占位。
5. `Tab` 全量采纳继续保留；`Ctrl/Cmd + →` 按下一标点渐进采纳，并在首次出现时提供一次轻提示，不常驻说明。
6. 继续输入、移动 caret、滚动或打开其他工具立即取消旧 Ghost；正文上下方向键不因 decoration 高度变化跳行。
7. 空行 `/` 或 Space 只归命令菜单；菜单和 Ghost 互斥，二级菜单按空间翻转；关闭后输入只执行一次。
8. 当前行、unit、Ghost、世界书匹配和选区按强调预算降级。

#### Gate

- 浏览前文连续移动不闪“正在联想”；稳定停驻后才出现一次；
- Ghost 与空格、`/`、IME、批注浮条不重叠；
- 全量与渐进采纳都保持正文排版和撤销原子；
- 第 80 段、0.85/1.0 UI zoom 下锚点仍在稿面内；
- ED-04 至 ED-09、AI-01/02/03 逐项通过主路径、完整状态和竞态边界；
- 只在整个交互闭环完成后跑一次 focused Gate 和 1440/390 局部截图。

### Slice V4：长推演成为正文中的“临时写作层”

**作者任务：** 在当前 writingUnit 下打开推演，指定方向，生成数段，删改两处，再纳入正文。

#### 工作

1. 入口位于目标 writingUnit 下方的单元间隙，名称统一为“推演”，不再叫模糊的“从这里续写”。
2. composer 首态只显示一行指令和必要选项；高级上下文、人物/对象选择按需展开，不先铺满表单。
3. 生成态占用预计草稿位置，避免正文突然上下跳；取消后间距完整复原。
4. review 使用与正文相同的版心、字体、行高、段距和缩进；用短来源线区分“尚未写入”，不做大面积蓝色卡片。
5. 保留编辑、草稿撤销、恢复生成稿、丢弃、采用编辑稿；动作贴近草稿末端但不挤入正文行。
6. draft 编辑不触发正文 Ghost、autosave、observer 和记忆候选；采用时一次性形成一个 writingUnit。
7. stale/error 不清空作者已改草稿；错误贴近动作区，重试不重新生成已成功文本。
8. 采纳后焦点落在新正文末尾，草稿层完全消失，段距恢复为普通 writingUnit 节奏。

#### Gate

- composer、生成、可编辑 review、stale/error、采纳后五种状态在同一锚点；
- 生成前后相邻正文间距一致，上下光标移动不别扭；
- 修改后采用写入编辑稿而非原模型稿；
- AI-04 至 AI-07 每个状态分别截图校对；stale/error 不能只由测试断言替代实图；
- 1440 主图经用户确认后再适配 1024 overlay 和 390 inline sheet。

### Slice V5：当前场 → 世界书 → 推演 → 正文闭环

**作者任务：** 当前场漏了艾德加；作者在“人物”中从世界书添加艾德加，选择“下一段入场”，以此推演，修改并采纳。

#### P1 C1-0 基线交接（2026-08-29）

- 已锁定基线为 `integration/consolidation-20260823` 的 `c55f3207` 加当前 dirty worktree；不能只以 commit SHA 代表运行页。
- 已建立独立的两章/三个 writingUnit fixture，包含当前场角色/地点/时间、世界书角色/地点、速记、叙事素材、有效记忆和 stale 记忆。
- 已从真实 5173 Authoring 页面输出当前页、现场编辑和生成前 AI 面板三张 1440 基线图；过程证据只保存在 `tmp/authoring-context-closure/`。
- 已确认旧“保存现场 / 保存并推演”混淆三种意图，旧“本次参考 N 项”只是生成后 ledger 占位，不是冻结的生成前预检。
- C1-1 先实施纯服务 `AuthoringRunSession` 合同，不与现有可见切片 owner 并发修改 `Authoring.vue`。
- 完整断链、owner 和下一刀见 [C1-0 真实基线与交接](../research/authoring-context-closure-c1-0-baseline-20260829.md)。

#### P1 C1-1A 冻结上下文合同（2026-08-30）

- 已新增内存型 `AuthoringRunSession`；返回值不保留 source document，只保留目标身份、共享当前场、合法候选、最终 manifest 和工具授权。
- 正文候选严格限制为目标前最多三个 writingUnit 与当前节点 caret 前缀；光标后正文、未来 writingUnit 和过远前文在 session JSON 中均不存在。
- 速记/素材最多三条，提交时按 project/source/revision 重新读取；`intent / fact / inspiration` 不再混成 fact。有效记忆沿用现有排序，但先严格排除 stale、inactive、跨 scope 和缺失来源 revision。
- 同一 canonical primary sourceRef 出现不同 revision、project、kind 或可序列化表示时拒绝整轮；同名 dependency 不同 revision 同样 fail-closed，outline additional 缺自身 primary revision dependency 时提交前拒绝。世界书 selection 只保留 entryId/matchReason，reader 按当前 project + `projection.worldbookId` 从绑定 repository 重读 live entry，最终条目引用仍唯一为 `worldbook-entry:<id>`。Compiler 的 included/excluded 均带作者可解释的安全元数据，但排除项不携带正文。
- 世界书和记忆工具只能从最终 manifest 的 `block.text` 合成最小只读索引；授权前复算 manifest 指纹，整本世界书、全量记忆、excluded、dependencies 和 provenance 都不能成为旁路。
- 已新增与冻结阶段共享 revision helper 的 C1 live collector：fresh session 对账为零差异，node/worldbook/asset/memory/scene intent 任一变化均精确 stale。记忆只守单一主来源 revision，并以 `memory-source:` 命名空间避免和显式探索/素材依赖冲突。
- Focused 41/41、上下文生命周期 6/6、定向 ESLint、diff check 与 production dry-run 1 项通过。此切片没有修改页面，因此三种现场意图、“本次参考”选择器和生成前后可见回执仍未完成。
- C1-1B 实施目标（现已完成，见下）：在长推演提交处创建 session，将其 manifest 与 manifest-only tool index 交给 executor/Kernel；模型返回后以实际序列化 receipt 对账，并从当前 repository 状态重读全部来源交给 C1 live collector 复核。禁止复用 token 形态不同的旧跨章 collector，也禁止把 frozen session 值当作 live revision。

#### P1 C1-1B 生产上下文闭环（2026-08-30）

- 生产长推演提交现只携带稳定 project/document/chapter/unit/node/caret target 与本轮 turn。薄 `AuthoringRunSessionAdapter` 在提交瞬间从当前 repository 重读 structured document、共享 `sceneProjection`、绑定世界书、显式参考、记忆和已采纳大纲；旧页面 `contextCandidates`、整章 narrativeContext、整本世界书和全量 runtime 不再是叙事真源。
- Authoring 的世界书命中关闭概率随机漏项，并把本轮指令纳入 discovery；绑定返回必须同时匹配 projectId、worldbookId 与实际 worldbook.id。旧 structured-settings-only 世界书的纯读归一使用确定性时间，连续读取不会凭空改变 entry revision。非法来源类型不会改写为素材，超过三条显式参考直接拒绝；记忆的未选素材、探索和其他章节 provenance 也会通过 repository 解析 live source revision。
- NarrativeKernel 在 manifest 模式下不再装配旧 scene/projection/cast/summary/recent/continuity/style，也不自行匹配世界书。最小工具索引只由最终 manifest 的世界书/记忆块生成；registry 只暴露索引中确有资源的 `world_lookup`、`memory_lookup` 与 BeatPlan，非 manifest 条目不能通过整本世界书或 runtime 扩权。
- provider 与 receipt 共用同一份最终 Kernel serializer。`ModelCallReceipt` 记录实际序列化的正文、当前场、设定和记忆块，以及 provider 截断/省略、compiler 排除、有界工具 evidence 与授权结果；provider 返回后再按 repository live state 收集 exact revisions。任一依赖变化都保留正文与 receipt 为不可采纳 stale 结果；工具 evidence 越权使用自身授权错误，不伪装为 stale。
- Ghost 保存该次冻结 session，采纳前再次重读 exact dependencies。“安排下一段”兑现从提前保存改为与正文、scene/outline delta 同批纯内存准备，全部校验后只持久化一次；保存失败原地重试，不再次请求模型。runtime revision race 同样保留生成正文和失效回执；stale 正文在目标处以只读、可选择复制、保留缩进且无横向滚动的预览显示。
- focused 4 文件 / 82 用例、上下文生命周期 6/6、Vite build、diff check 与 production dry-run 1 项通过；提交仍以整棵树最终 `verify:full` 为硬门禁。本切片只补生产可信链和 stale 可见性，不宣称三种现场意图、本次参考选择器或生成前后作者摘要已经完成。
- 下一刀 C1-2/C1-3：复用已有待入场链，明确“纠正当前 / 安排下一段 / 仅作用本次”；增加最多三条的速记/素材选择器，并用作者名称显示“将参考 / 实际参考”。完成后再做 1440/390 真实旅程与视觉校对。

#### P1 C1-2 现场三意图闭环（2026-08-31）

- 新增按 project/chapter/writingUnit 隔离的内存型 scene run intent。人物和地点均可直接选择“安排下一段”或“仅供本次参考”；“保存当前场”是唯一纠正当前动作，旧“保存并推演”不再混合事实保存与运行意图。若表单仍有未保存的当前场纠正，临时动作会 fail-closed，要求先保存或取消。
- 临时意图只携带稳定 ID、作用域、世界书 revision 与 canonical `worldbook-entry:<id>`。session adapter 从绑定世界书精确重读条目并把它纳入 manifest 与最小工具授权，不依赖关键词 matcher 碰巧命中；非法 payload、跨实体或非 canonical ref 均在 reader 阶段拒绝。
- `next-passage` 只有在最终 manifest 实际入选且 receipt 确认完整序列化时，才在 Ghost 采纳事务中形成显式人物/地点 effect，并与正文和其他 scene/outline delta 一次持久化；`run-only`、本次移除、packing 排除和 provider 省略/截断均不形成 scene delta。在场人物 8 人上限由 UI、意图创建与采纳事务三层共用，不能写入第 9 人再由投影静默截掉。作者修改 Ghost 不会抹掉合法明确安排，保存失败保留草稿、receipt 与 intent，重试只重试保存。provider 失败和 Ghost 编辑期间保留；放弃、成功采纳及书/章/构思/世界书切换清除，迟到结果仍受 frozen target 与 live revision 门禁。
- **计划交接（2026-09-05）**：09-05 UI/UX 计划接管后续实施，A2-1 Worldbook 代表片已实施（内容优先 + AI 规则折叠 + 对象属性紧凑行），详见 09-05 计划 A2-1 交付记录。
- 真实 5173 V5 旅程 5/5，明确断言采纳前 localStorage 与左栏现场零临时写入，采纳后编辑稿和艾德加入场同时落地，且 console/page error 为零。390px 复验确认候选区为普通列表 + 按压态、移动动作 44px 逻辑高度、零横向溢出。Focused 3 文件 / 59 用例与最终 `verify:full` 全绿：20/20 文件、200/200 用例、Vite/VitePress build、diff check 通过。
- 下一刀 C1-3：新增独立的轻量 reference controller/picker，最多选择三条速记/素材，并把冻结 manifest 和实际 receipt 以“将参考 / 实际参考”的作者语言展示；不把素材管理页搬入工作台。

#### 计划交接与新增修复（2026-09-05）

用户批准 [09-05 UI/UX 与故事试演计划](../plan/authoring-ux-and-story-play-plan-20260905.md) 接管后续实施，本计划 V6/V7 由该计划 A2 波次承接。交接时实施三项修复：
- **空章节打字失效（P0）**：后续会话的 gap 重设计（0→54px 实体块 + stopEvent 吞事件）覆盖了空章节首行——gap 容器改 `pointer-events: none`、按钮/表单恢复 `auto`
- **UX-03 落地**：检查器"现场"页复刻的四行交互索引替换为只读紧凑概览（时间/人物/地点 + 调整当前场/推演动作），不再与左栏重复
- **场景流内核 payload 超限**：`narrativeKernelPayload` 是单 text part（合同上限 8000 字符），场景块加入后必然越限——按块 2400 字符界额截断

A1-R 阻断项（720×450 助手输入遮挡）实测已被先行会话修复（四尺寸复验通过）。回归：acceptance 19/19、V2 Gate 5/5（探索定位已适配 IdeaShelf）、vitest 200/200。V6/V7/V8 不再单独执行，由 09-05 计划统一接管。未提交。

#### P1 C1-3 本次参考与作者可读回执（2026-08-31）

- 新增独立 run-only reference selection 合同。当前场详情与推演区共用同一轻量选择状态，候选只分“速记 / 素材”，最多三条；不提供归档、删除、改标签或转世界书，只保留完整页面入口。选择不改变来源正式状态，也不写新的长期 localStorage。
- 选择冻结点击时 revision 与用途。探索速记和普通素材默认 inspiration，剧情事件为 intent，只有显式人物事实素材为 fact；重复、第四条、跨项目、删除和空 revision 均 fail-closed。来源在选择后修改时，生产 adapter 不再悄悄换成 live revision，而以旧 revision 进入 reader 并显示失效；作者确认“采用新版”后才更新。
- 推演区的“将参考”不是页面估算：它直接调用与提交相同的 `prepareSession()`，按当前 target 与输入指令重新读取正文、当前场、世界书、作者参考、记忆和大纲，再执行统一去重、冲突与预算。核对期间主动作暂不可用；展开清单只显示作者语言的六类来源、加入原因、截取/冲突/排除状态和“本次排除”。
- 生成后的 AI 详情只显示 receipt 驱动的“实际参考”，区分实际使用、篇幅省略、版本失效和冲突压制；内部 profile、token、candidate ID 与字符预算退出作者界面。stale 结果继续保留不可采纳正文与实际回执。
- focused 3 文件 / 58 用例及真实 5173 C1-3 旅程 14/14 通过。1440 截图覆盖当前场选择、冻结预检、Ghost + 实际参考；390 sheet 可滚动完成选择；两个宽度均为零横向滚动、零 page/console error。最终 `verify:full` 全绿：20/20 文件、200/200 用例、Vite/VitePress build 与 diff check 通过。

#### P1 C1-4/5 当前 writingUnit 重写（2026-08-31）

- 复用现有块间 composer，以克制的行式任务切换提供“推演下一段 / 重写当前块”；不增加第二个生成入口或独立重写面板。两种任务共用本次参考、生产 session 预检、receipt 与可编辑 Ghost。
- 普通推演仍只允许目标节点 caret 前缀；显式 `rewrite-unit` 才把目标 writingUnit 全文作为“待重写文本块”进入 manifest，且未来 writingUnit 仍不可见。next-passage scene intent 不进入重写 session，run-only 设定可以保留。
- 采用重写稿时原位替换整个单元，保留 unitId 与单元数量；作者编辑后的文本是唯一写入内容，scene/outline 派生效果和“安排下一段”不随重写兑现。放弃继续零写入，保存失败继续保留锁定 Ghost 并只重试持久化。
- 替换前后保存精确 schema-v3 单元快照。撤销/重做使用 sidecar receipt 恢复快照并持久化，不把保留 unitId 的替换误判为一次插入/删除；本次观察器用独立 ghost-adoption 来源失效，避免撤销误伤该 unit 既有记忆。
- focused 2 文件 / 52 用例、Vite build、diff check 与真实 5173 旅程 23/23 通过。1440 覆盖预检、可编辑 Ghost、原位替换和撤销；390 任务切换保持正文宽度且零横向滚动，两个宽度均零 page/console error。最终 `verify:full` 仍是提交硬门禁。

#### P1 C1-6 故障注入（2026-08-31）

- 新增隔离浏览器故障 Gate，不改产品 UI：下一段与重写 Ghost 编辑后放弃均保持正文、现场、大纲、素材和记忆零写入，也不触发正文观察器。
- provider fixture 可按 plan/prose/critic 阶段注入延迟或 typed HTTP 失败。空正文与 timeout 保留作者输入；生成中切换章节会关闭旧 composer，迟到响应在正常离页保存之后仍为零额外写入；Ghost 发布后目标 writingUnit revision 变化会保留草稿但阻止采纳。
- `writing_books` 首次保存失败时，已插入的内存事务锁定在 Ghost 中且观察器尚未调度；“再次保存”只重试持久化，不再请求模型、不重复插入，成功后只调度一次观察器。撤销下一段只失效新 `unit:` 来源；重写继续按本次 `ghost-adoption:` 来源失效，不影响旧记忆。
- 真实 5173 故障矩阵 31/31、focused 2 文件 / 36 用例与最终 `verify:full` 全绿：20/20 文件、200/200 用例、Vite/VitePress build、diff check 通过。C1 下一刀只做 C1-7 三张真实页面最终视觉/鲁棒性验收。

#### P1 C1-7 最终页面验收（2026-08-31）

- 新增唯一最终浏览器 Gate，直接测量左右工具区域固定、详情开关前后的正文 selection/scrollTop、目标 writingUnit 与 Ghost 内容轴、1024 覆盖层、390 sheet 边界/独立滚动/44px 逻辑触控和三视口横向溢出；不再以分散阶段截图代替最终旅程。
- 只保留三张正式证据：1440 当前场+世界书人物/地点+三种意图+本次参考，1440 可编辑非正文 Ghost+实际参考，390 右侧详情 sheet。中间报告和失败诊断继续写入 `tmp/`。
- 最终页面 Gate 24/24，参考/重写旅程 23/23、故障矩阵 31/31；`verify:full` 20/20 文件、200/200 用例、Vite/VitePress build 与 diff check 全绿。C1 P1 自动化验收完成，仍等待用户审美确认、真实 provider 和 Windows 原生中文输入法耐久外部门禁。

#### 领域语义

当前场修改必须先区分：

| 意图 | 作用 | 是否立即改变当前现场 |
| --- | --- | --- |
| 纠正当前 | 修复当前落笔处已经成立但漏检/错检的事实 | 是，写 author anchor |
| 安排下一段 | 约束接下来的推演和大纲 intent | 否，采纳后才兑现 |
| 仅作用选段 | 只给本次改写/推演参考 | 否，run-only |

#### 工作

1. 左侧点击“人物”在右侧打开现场人物详情，不跳去完整设定页。
2. 详情顶部先显示已在场人物和角色；“添加人物”打开绑定世界书的角色选择，不复制世界书编辑表单。
3. 选择人物后要求明确“纠正当前 / 安排下一段 / 仅作用本次”；默认推荐“安排下一段”，但不静默替作者决定事实。
4. 人物详情可展开世界书摘要和当前文本证据；编辑完整设定时打开对应世界书 workspace tab。
5. 设定词匹配范围以 caret 之前的当前段 + 前几个相关 writingUnit 为主；不扫描后文，不以“调用远近”等技术术语直接展示。
6. 匹配到的莉娜、艾德加、税务所只在正文中轻下划线/页边信号；点击词汇打开右侧对应详情，常态不铺彩色底。
7. “以此推演”把人物、作用范围和作者指令传给 V4 composer；作者仍可修改生成稿。
8. 丢弃时当前场和大纲零变化；采纳时正文 + 明确选择的 scene delta + outline fulfillment 使用既有原子事务。
9. 采纳后左侧当前场简报更新人物/事件，右详情保留来源回执；保存失败保留可重试草稿，不重新调用模型。

#### Gate

- 完整完成“左侧人物 → 世界书选择 → 下一段入场 → 推演 → 编辑 → 采用 → 当前场更新”；
- 纠正当前和安排未来不会混淆；切回前文不读取后文 anchor；
- 世界书条目只有一个详情 owner，不在当前场复制维护页面；
- SW-01 至 SW-07 逐项完成 UI、完整性、鲁棒性和用户截图验收；
- 一张 1440 全链截图 + 一张正文词汇局部图 + 一张 390 人物 sheet。

### Slice V6：右侧工具逐个收口

右 rail 首轮只保留六个真实内容入口：

```text
批注 / 大纲 / 设定 / 现场 / 历史 / 上下文
```

- `角色` 合并进现场人物；
- `素材` 移入“更多”或独立素材 workspace tab；
- `双栏` 移到 inspector 头部，只有支持双栏的面板可见；
- 不删除底层数据和完整页面，只调整文本工作台默认入口。

每个工具是一个独立子切片，完成前不同时重画另一个面板。

#### V6.1 批注

- 右栏壳固定，只有批注条目按正文 anchor 重新定位；滚动时外壳不移动。
- 无批注时只给一句空态；选区后 composer 出现在对应边注位置。
- 条目采用开放边注排版，编辑、按批注改写、删除靠近条目；不把每条变成完整卡片。
- 批注详情、改写候选和 diff 使用同一 Review Tray 语言；关闭后焦点回到原选区。

#### V6.2 大纲

- 默认展示当前章节邻域和项目层级，不把全部节点平铺成条目墙。
- 列表负责选择和编排，详情负责意图、关联探索、章节映射和状态；双栏模式才并列。
- 草稿探索可挂到未归章节点；正文映射显示 `第一章　章名`。
- 调整大纲不直接改正文；已采用/待采用/搁置使用短状态线，不使用大面积卡片。

#### V6.3 设定

- 第一层显示当前落笔处真实命中的条目；范围说明使用作者语言，例如“当前段与前 2 个片段”。
- 第二层按人物、地点、规则和其他分组；组内优先显示当前文本命中、当前场引用、作者钉选，再显示其他候选。
- 详情显示摘要、匹配词、证据片段和“用于本次/加入当前场/打开完整设定”；不把所有条目一次铺开。
- 设定词高亮只来自实际 match receipt；没有命中不伪造强调。

#### V6.4 现场

- 复用 V5 的同一 projection，不在右侧重新实现一份当前场。
- 概览按人物、地点、时间、未决事件分区；点击进入单条详情，提供纠正、安排和推演动作。
- 机器检测与作者确认分层；机器结果不能伪装成已成立事实。

#### V6.5 历史

- 默认只显示当前修订、未保存恢复稿、最近三份章节快照和最近相关片段历史。
- 创建快照、恢复、删除保持明确；恢复前说明影响范围并自动保留当前状态。
- 长列表内部滚动，不改变正文位置；探索与正文章节作用域明确。

#### V6.6 上下文

- 不做 Agent 聊天壳，只显示本次生成真实使用和排除的来源。
- 第一层用作者语言概括：当前正文、当前场、世界书、已采用探索、相关前文章节。
- 章节参与上下文必须可见：当前章自动；相关前章可包含；未来章节默认排除；作者钉选未来内容只作为“计划参考”，不能伪装已发生。
- 第二层才展示具体来源、字符预算、截断/排除理由和 receipt；技术字段放折叠详情。
- 没有真实 compiled manifest/receipt 时显示“本次尚未生成”，不根据页面状态猜 ledger。

#### V6 Gate

- 每个入口都有 empty/loading/error/success 和一个真实主动作；
- 切书、切章、切探索后不显示旧作用域；
- rail 图标数量和分组稳定，所有 tooltip/name 与详情标题一致；
- RT-01 至 RT-09 与相应 AI/SW 功能逐项走完基线、两轮校对、边界状态和用户确认；
- 每个子切片只保留最多 2 张最终成功截图，但允许反复截图返工；用户逐个确认密度、层级和完善性；
- 六个子切片完成后才跑一次右栏 focused Gate，不逐面板跑全量。

### Slice V7：固定侧栏、响应式与缩放

**作者任务：** 在 5000 字章节中滚动、打开各工具、缩放、切换宽度，再回到原 caret。

#### 固定/跟随矩阵

| 对象 | 正文滚动时 | resize/zoom 时 | 窄屏 |
| --- | --- | --- | --- |
| 左导航壳 | 固定；内部独立滚动 | 保持列宽策略 | chapter sheet |
| 当前场简报 | 固定左下 | 高度不足时压缩摘要 | scene sheet |
| 右工具 rail | 固定 | 始终贴边 | bottom rail |
| 大纲/设定/现场/历史/上下文 | 外壳固定，内部滚动 | overlay/停靠重算一次 | mutually exclusive sheet |
| 批注条目 | 右栏壳固定，条目随 anchor 重排 | 合法则重排，否则收起 | sheet 内顺序列表 + 回到原文 |
| Ghost/命令/选区条/长推演 | 跟随文本 anchor 或关闭 | 重新定位或关闭 | 正文内/底部受控层 |

#### 工作

1. 将 `.wall__dossier-scroll` 固定为桌面正文唯一纵向滚动 owner。
2. 页面壳结构继续由 `Authoring.block-native.css` 负责；≤720/980/1180 断点只在此处定义一次。
3. 子组件不再通过父页面的旧 scoped selector 偶然获得 `display`、尺寸或定位。
4. 统一 visualViewport、CSS zoom、caret rect 和 overlay bounds；滚动时无法合法重排的临时层直接关闭并恢复焦点。
5. 1024 inspector 覆盖正文但不改 grid；390 章节、现场、工具详情互斥，底部 rail 不遮正文最后一行。
6. 200% zoom 保持所有核心动作可达；允许极窄有效视口重排，但不能在宽屏物理截图中误退化为普通文档流。

#### Gate

- 1440/1024/390 × normal/200% zoom 无整页结构崩塌；
- 左右工具不随正文移动；文本锚定层位置正确；
- 无横向页面滚动、正文双滚动、rail 遮挡和 sheet 焦点泄漏；
- RS-01 至 RS-08 必须逐项验收，不能只用三张静态截图代表全部响应式与竞态；
- 代表截图外再输出几何报告，不用肉眼猜 fixed/anchor 是否正确。

### Slice V8：删除重复 owner、性能与最终用户验收

#### 工作

1. 根据 V1–V7 已确认结果删除内部重复书页签、旧 right-tree、无消费者 Ghost、退役素材/角色 rail 壳和对应不可达 CSS。
2. 按 ownership 清单迁移并删除三层 CSS 的重复 selector；不是简单追加更高 specificity。
3. 只在确实形成稳定职责时抽取 `Authoring.vue` 子组件；不以减少行数为目标重构业务逻辑。
4. 检查长文输入、Ghost decoration、批注重排、当前场匹配和 inspector 切换的布局抖动与重复计算。
5. 清理过程截图、失败 trace、临时 fixture 和审计目录；只保留用户批准的代表证据。
6. 更新计划、STATUS、已知问题和实际完成边界；真实 provider 和 Windows 原生输入法未验收时必须继续标为外部门禁。

#### 最终验收故事

作者从 AppShell 中打开《雾港纪事》，直接进入第五章。顶部只有紧凑的写作工具，左侧能快速在构思和正文之间切换，中央是一张连续、安静、排版正确的中文稿面。作者回看前文时没有联想闪烁；停在某句数秒后出现轻量 Ghost，可逐段采纳。作者在当前 writingUnit 下打开“推演”，先修改生成的多段草稿。

发现现场漏了艾德加后，作者从左侧当前场人物进入右详情，从世界书选择艾德加并安排下一段入场，以此重新推演。丢弃不改变事实；采用编辑稿后，新 writingUnit、入场变化和大纲兑现一次保存。批注、大纲、设定、现场、历史和上下文都从同一右 rail 进入，左右栏不随正文滚动，只有真实锚定内容跟随文字。

#### 最终 Gate

- 用户逐切片确认 V1、V2、V4、V5 和 V6 代表截图；
- Windows 原生中文输入法 30 分钟耐久：输入、回看、上下移动、Space、`/`、Ghost、批注、推演；
- 真实 provider 只跑短 Ghost 与长推演 canary，空返回单列 provider failure；
- 完整旅程覆盖上述最终验收故事；
- 最后只运行一次 `npm run verify:full`；必须在 20 测试文件 / 200 用例预算内；
- 默认不为视觉微调创建提交。一个经用户确认的可见切片才是候选提交边界；未经用户要求不提交。

## 8. 实施与验证节奏

| 阶段 | 允许动作 | 验证 |
| --- | --- | --- |
| 计划/设计 | 只读代码、真实截图、状态表、硬约束 | 不跑测试 |
| 一个切片实现中 | 连续完成数据选择、交互、主要布局和失败状态 | 静态检查与人工浏览，不因小改反复启动测试 |
| 切片 Gate | 当前作者任务形成闭环 | 一次 focused test + 最多 3 张代表图 |
| 用户视觉确认 | 根据截图只调当前区域 | 按功能反复校对直至确认；只保留基线与最新版 |
| 2–3 个切片阶段组 | 清理相邻回归 | 一次组合 journey，不跑全量 |
| V8 最终交付 | 文档与代码收口 | 一次 `verify:full` |

实现过程中，如果测试提前暴露编译失败、schema 无法加载或页面白屏，可以立即跑阻断检查；除此之外不把测试频率当质量指标。

## 9. 文件所有权与修改边界

### 页面级 owner

- `Authoring.block-native.css`：Authoring 页面高度、顶栏、主 grid、列宽、背景、断点、固定/overlay/sheet 结构；
- `Authoring.vue`：页面编排、领域状态接线、跨组件事务；不继续承载新组件内部视觉细节；
- `Writing.global.css`：只保留 Notebook/ProseMirror 真正跨组件共享的排版和文本标记；
- `Writing.scoped.css`：现有旧规则按切片迁出或删除，不再作为 Authoring 结构安全前提。

### 组件级 owner

- `WritingNotebookEditor.vue`：正文 surface、writingUnit、caret/selection、Ghost、命令菜单；
- `AuthoringBlockComposer.vue` / `AuthoringBlockDraft.vue`：长推演输入、生成、review 和动作；
- `AuthoringSceneRail.vue` / `AuthoringInspectorDetail.vue` / `AuthoringSceneCuration.vue`：当前场简报、详情和编辑；
- `AuthoringOutlinePanel.vue`：大纲列表/详情；
- `AuthoringWorldbookPanel.vue` / setting components：当前落笔处匹配和设定详情；
- 批注、历史和上下文若继续留在 `Authoring.vue`，先稳定视觉职责；只有职责确认后再抽取，避免边做设计边搬状态。

任何切片不得让两个实现者同时修改 `Authoring.vue`、Notebook 或核心 Authoring CSS。视觉方向需要连续判断，默认由一个 owner 完成当前区域。

## 10. 暂停条件

1. V1 中央稿面截图未获用户确认，不进入左右栏；
2. V2 左导航/当前场层级未确认，不实现完整现场闭环；
3. writingUnit 可见语法未稳定，不继续增加块级动作；
4. V4 长推演仍像卡片或造成段距跳变，不接世界书/现场；
5. V5 未区分纠正当前与安排未来，不允许 scene delta 自动写入；
6. 某右栏工具没有真实 owner/状态/主动作，先隐藏，不用占位补齐；
7. 真实页面和自动 fixture 表现不同，先查运行 branch、数据和 CSS 产物，不用更多覆盖规则掩盖；
8. 用户指出当前切片方向错误时，只回退或调整该切片，不借机重做未确认区域。

## 11. 完成定义

本计划完成不是“页面看起来比以前整齐”，而是以下事实同时成立：

- 作者打开书后第一眼看到可持续写作的中文稿面；
- 构思、正文和当前场在左侧形成紧凑、连续、可理解的关系；
- writingUnit 真正承载一段或数段完整文字，但不把正文切成卡片墙；
- 短 Ghost、空行命令、长推演和批注互不抢输入，且在正文中位置自然；
- 当前场可以从世界书添加人物/地点，并真实驱动可编辑推演和采纳事务；
- 六个右侧工具逐个具备真实内容和统一视觉层级，外围素材不干扰核心；
- 左右栏固定，文本锚定层跟随，1440/1024/390 与常用缩放都不崩；
- 每一个完成结论都有对应真实页面、作者任务、截图和操作结果，而不是只有测试或代码说明。
