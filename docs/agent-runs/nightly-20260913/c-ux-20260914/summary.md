# C 线摘要与残余(c-ux-20260914)

实际起止:2026-09-14 00:10 前后启动(Asia/Shanghai),约 01:55 封板;单会话顺序实施,无中途退出。

## 本夜作者可感知变化(先看这五件)

1. **回访作者一进首页就能点“继续《书名》”**,直达最近一本书;手机第一屏可点,不再先穿过欢迎语(390/1440 均验证)。
2. **首次指引不再“建了推演就算完”**:发起试演后提示挪到推演面板,按真实进度提示等待回应/可写试稿/试稿仍可修改;关闭按书记住,更多菜单可重开。
3. **导入不再吃掉作者的手改**:书名改过不被重解析覆盖;章名手改按内容对应保留,换拆分方式时明示哪些没保留并可一键撤销;quota 失败错误留在弹窗、预览可重试。
4. **保存失败有了自救条**:重试保存 / 导出当前正文(实时含未保存修改)/ 查看恢复稿;刷新后恢复稿入口仍可达;推演失败同样就地给出中文原因、重试与检查模型连接,关闭设置后焦点回到原入口。
5. **备份说人话**:存储分区先讲备份再讲技术键表;恢复前展示备份时间与包含的书稿;恢复成功直接给“回到书稿”回程(单本书直达,多本回列表)。

## 候选与复验

- 分支/树:`night/authoring-ux-20260913` @ `813d7f2`(8 个实现提交 + 1 个 Gate 增量,基线 main@5152aad)。
- 分线封板:`npm run verify:full` exit 0(20/200 预算内全绿、双 build、diff、docs);三个既有 Gate(web-beta / rehearsal-panel 304、settings-linkage 20)全绿;六个 C 行为检查共 56 项断言 0 失败。详见 `verification.md`。

## 仍失败/未完成/需要决策(按优先级)

1. **blocked-external(A 线缺席)**:后果 DTO 对照 UI、“补充本次条件”baseline 冻结接口、A 缺席下 J06–J11 的浏览器纵切。C 已交付失败就地回程段,未用自由文本冒充结构化后果。O 组合 A 合同后由 C(或持同等写权者)按 `verification.md` 的接线点补全。
2. **跨线决策**:全局 `MemoryIndicator` 浮动胶囊在 390 挡内容(基线图 `c00-welcome-books-mobile.png`);文件不在 C 写集,请 O 指派。
3. **未开始储备**:C10 示例书(依赖 B07)、C11 目录小屏、C12 历史面板抽取;时间用于封板与 Gate 维护。
4. **残余小项**:长章名现为省略号可供性(input 单行),换行式标题需后续专项;lint 存量(block-order 等)归 B。

## 2026-09-14 集成纠正

- 原第 1 项跨线阻断已解除：A 的本次条件、结构化后果、路线差异与冻结试稿来源均已接入现有右栏，没有新增工作区或卡片体系。
- 推演失败不再直接展示英文 transport 文本；作者先看到中文行动建议，原始错误折叠在“技术信息”中。后果校验失败时还可阅读回应，并显式决定只保留回应或整次弃掉。
- 人物选择从姓名改为稳定 ref，解决同名人物在 UI 层仍可能串选的问题；窄屏继续沿用顺序展开与 44px 触控规则。
- 真实样本显示该闭环是“可恢复但未全绿”：12 次单步均返回，4 次后果需人工无后果降级；3 次试稿中 2 次完成、1 次双重超时。详见 A 线回执的集成纠正。
