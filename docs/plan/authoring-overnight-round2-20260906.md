# 第二轮夜间任务书：稳定写作、可改试演、资料能力接入

日期：2026-09-06。状态更新（2026-09-07）：**已执行、部分交付，未整体验收通过；后续由[第三轮任务书](./authoring-overnight-round3-20260907.md)接管**。以下保留第二轮原定目标与范围，不代表已完成。原计划承接 main `6b7a017`；不从原目录的旧 integration/WIP 分叉。

从属 [体验计划](./authoring-ux-and-story-play-plan-20260905.md) A2–A4 与 [并行能力计划](./authoring-parallel-foundation-plan-20260905.md) K/I0/E0；不替换产品路线。前轮事实见 [合并回执](../agent-runs/overnight-merge-20260906.md)。

## 1. 这次交付什么，为什么不会再是两小时短单

两条独立队列，各以约 8h 为运行上限，**不是精确工时承诺**。核心目标是早晨能验收两个完整变化：

- **U：写得稳，也能玩一次完整的“改要求→再试→采用/放弃”**。先关选区/IME/搜索返回缺口，再做现有试演的作者追加要求、失败与旧稿保留闭环，不再仅调几处折叠和文案。
- **K：从离线函数走到现有资料助手的受控接入候选**。先补授权快照和真实源映射，再在默认关闭的窄接缝验证来源、未知、过期和回退；不是再写一遍 K0–K4，也不是造数据库。

每线有必做、目标、已列明储备。执行本计划时，储备包也属于范围，满足前置即可继续，不用等用户醒来逐包批准；但本文列出的外部门禁不能自动跨越。完成必做项不是停止条件，卡住某包不等于整线停工。

**容量校准，不再伪装估时精确**：U 有 6 个主包 + 3 个储备包，K 有 6 个主包 + 3 个储备包。按首次安全复验、跨层接缝和真实浏览器取证估算，每线主包可能需 5–9h，储备另有约 3–6h；这是粗略任务容量，不是实测生产率。启动 60–90min 后用实际完成速度重估；若仍可能 2h 耗尽，立即转入已列明储备，而不是把重复测试算作新工作。

最低可合并交付是安全底线，不是成功终点：U 至少 U01–U03 收口；K 至少 K21–K23 收口。晨间应逐项写出目标包是否做到；只达底线必须标“部分交付”，不能称整轮完成。

## 2. 现状证据与不重复做的事情

本轮代码核对，不把候选缺陷直接宣布为已证实漏洞：

| 证据 | 当前事实 | 本轮处理 |
|---|---|---|
| 合并回执、`authoring-journeys-smoke.mjs` | 核心 200/200；完整 local journey 的 J9/J11 仍失败，J1 单轮过不等于间歇问题消除 | 先建立失败分母和因果链 |
| J9 compositionend 后 waitForFunction；Notebook 的 capture/settling 逻辑 | synthetic composition 与真实输入法不同，4000ms 超时不能直接归咎模型 | 区分测试构造、PM composing、应用 owner；不得只加 timeout |
| `Authoring.vue:closeSearchPanel` 与 J11 | 查找定位后不恢复旧位置是有意设计；测试还调用 Vue exposed selectText 强制重建选区 | 修真实返回合同，移除对实现私有补救的依赖 |
| `AuthoringBlockComposer.vue` | 最终版本为展开操作/类型布局，人物 ID 已在合并修复 | 不重新做“折叠/展开”审美摇摆 |
| `AuthoringBlockDraft.vue` | 已有结构折叠；段落不等于写作单元 | 不用段落数断言替代单元边界合同 |
| `knowledgeReadModel/query.js:createKnowledgeScope` | scope 外壳冻结；snapshot 冻结由可信调用方负责，alias index 派生于该快照 | 审调用边界，不能仅凭浅冻结就声称生命周期安全 |
| `authoringKnowledgeQuerySession.js:prepare` | 已有 repository snapshot、through-target、required refs、evidence envelope、projectRevision | 复用这条链，不建第二套问答/检索/UI |
| K eval / benchmark | 46 个合成场景已过，1k p95 约 1.6ms；rich 能力不等于旧档字段存在 | 做旧形状与接缝，禁止用扩充合成样例冒充上线 |

继续保留：Worldbook 内容优先；Character/Outline 桌面结构；手机目录详情堆叠；标题优先于“已保存/交换/关闭”；正文是视觉主体；纯写作不需要模型。

## 3. 开工门禁：失败就别冒称两线已经跑起来

由启动 owner 完成以下清单，worker 不自行猜测：

1. 在 `/home/recoletas/jiuguan/pinax-integration-20260906` 核对 main；先保存/提交本任务书与共享安排，再记录共同 **BASE SHA + 任务书版本/hash**。基线应包含 `6b7a017`，且无未知 WIP。旧 `text-game-framework` 仅保留，不作为开发源。
2. 新建两个独立 worktree/分支，建议 `night/round2-u-<run-id>`、`night/round2-k-<run-id>`。两个 worker 不得在 main 或旧共享工作区写代码；启动者验证 `git rev-parse --show-toplevel`、分支、干净状态，而不只写“请独立”。
3. 各工作区有自己的测试输出和 Vite cache；不要复用可被另一线重写的 `node_modules/.vite` 缓存。可复用依赖安装，但必须隔离可写缓存；需要依赖修复时限制在自己环境，不能改锁文件或用户安装。
4. 启动 owner 登记两线测试端口、PID、源码根。允许自建 loopback 测试服务；不启停用户 5173，不碰旧服务。K 若进入 I0 浏览器 Gate，使用自己的前端源。
5. 使用全新浏览器 context + 合成 fixture，**网络拦截必须在页面导航前安装**。生成请求仅能进入明确 mock；外部 HTTP/WebSocket 默认拒绝并计数。不要用“后端不可达”作为无真实调用保证。
6. 禁止默认运行未隔离的 `JOURNEYS=all`：旧 backendAvailable 对 BASE 的字符串端口替换不可靠。启动自检必须证明 J6/J7 不会误连真实 provider，未通过前仅运行显式 local/mock。
7. 记录具体开始时间、截止时间和冻结时间（T+6h）。启动者提供可恢复的长程执行方式，并实测一次正常子任务退出后可读进度进入下一包；**只把一段提示词扔给单次会话，不算已配置整夜执行**。如果续接机制不可用，报告单次执行模式，不承诺无人值守 8h。
8. 共享任务板由集成 owner 维护，worker 只写自己的交接目录。无需夜间第三条实现线；owner 负责启动/接缝裁决/汇总，不与 worker 同时改业务文件。

启动任务板最少字段：run-id、base、plan hash、owner、worktree、branch、port/PID、写集、当前包、开始/截止/冻结时间、最终 SHA、证据目录、阻断、下一包。任务板放 `docs/agent-runs/<run-id>/board.md`，不覆盖其他历史任务板。

## 4. 接续、转向与冻结协议

每包状态只能是 `queued / running / done / blocked / deferred / not-needed`；not-needed 必须有现状证据和等价验收结果，不能凭“感觉已经不错”略过用户路径。

每包循环：读进度 → 核对前置/写集 → 复现或基线 → 最小实现 → focused/专项 Gate → 自审 diff → 记录结果 → **自动领取下一个就绪包**。

- 每包结束更新进度；长包每 30min 写一次。记录墙钟起止、实际操作区间、等待/工具耗时和产物，不以“会话存在多久”冒充有效工作。
- 60–90min 做一次容量检查：主包提前通过即取储备；不等预定整点。只可推进本计划列明的独立包，不自行新增领域。
- 连续 30min 无新证据，必须换诊断方法；一个疑难包最多 90min 后形成最小复现和转向说明。后续若有新证据可再开一次有界修复，不机械重复失败命令。
- 普通阻断只阻断依赖它的包。U 的 IME 问题未解，可以做静态层级/只读评测；不能放行受影响的输入新玩法。K 授权门禁不通过，I0 不接线，转 K-R 包。
- 发现越权、正式数据误写、旧书串写或不可恢复输入丢失，立即隔离本包，优先恢复基线安全行为；该路径不得标 ready。安全事件不会授权全仓库重写。
- T+6h 后停止领取新功能包；允许修复本轮回归、完成矩阵、整理证据和提交。预计剩余时间不足“实现 + 专项验证 + 全量验证”时，不开下一包。
- 正常提前结束仅有：所有主包和所有就绪储备已完成；或者其余全部有具体外部阻断且无独立任务。必须逐项列出耗尽/阻断理由，标“提前队列耗尽/部分交付”，不静默结束。
- 冻结后任何代码修改使旧验证失效：重跑受影响 Gate + 最终 verify。摘要绑定代码 tree 和测试命令，不靠“昨天绿过”。
- 截止时只隔离自己的未完切片，不丢作者/其他线数据；不得用删测试、改成 skip、私有函数补选区或降低安全断言制造通过。

## 5. U 队列：可靠交互 + 一次真正可控的再试

U 的顺序是 U01 → U02/U03 → U04 → U05 → U06；U-R1/R2 可独立穿插，U-R3 在 U05 后。建议主包实现约 4–6h，最后 2h 收口；这是容量参考，依 §4 实测接续。

### U01 · 测试隔离与失败基线（必做）

- 入口：`scripts/authoring-journeys-smoke.mjs`、`scripts/authoring-ui/{runner,provider-mock}.mjs`。
- 交付：一条明确、可重复的本地入口；修正/隔离后端探测，未明确 opt-in 时不进入真实生成。记录 mock 命中、意外网络、源树、端口和所有失败。
- 验收：J1/J9/J11 各先完整运行一次，保留失败现场；其他 local 项作为控制。故意放入一个未模拟生成请求，Gate 应拒绝而不是转真实后端；故意制造断言失败，最终 exit 必须非零。不可删掉真实模式，只给它明确显式边界。
- 下游：环境通过后启动 U02/U03；若浏览器启动失败，转 U-R2 的离线评测，并报告本轮 UI 不具备交付门禁。

### U02 · 输入法与右键选区 owner 收口（必做）

- 入口：Notebook 的 composition capture/settling、`focus`、空白点击；页面右键恢复与全局清理。
- 交付：区分 DOM/PM selection、activeElement、composition owner 的时序证据；修 J9 根因或验证测试构造不合法并修测试驱动。业务选择权留给真实输入法，不强制结束 composition。
- 覆盖：空章、已有正文、正/反向选区、文末空白、主/副栏、Esc/Enter/Tab、菜单开关与切章；书签 stale 必须拒绝编辑动作。不得将“归还焦点”当“目标仍有效”。
- 验收：J1 20 次全量记录；synthetic J9 至少 10 次，正文/结构不变、无误采纳、组合结束后可继续输入并持久化。真实 Windows IME 未实测明确标待验；单次绿色不能消除间歇记录。
- 失败转向：U-R1 静态可达性或 U-R2；涉及 IME 的 U05 编辑路径保持 blocked。

### U03 · 查找返回到作者刚选的位置（必做）

- 入口：`closeSearchPanel`、搜索结果导航、活动编辑面恢复、选区浮条。
- 冻结合同：未导航时关闭回到进入前位置；已点结果时关闭留在结果位置/活动栏，不跳回旧章。面板打开时可抑制浮条；关闭后有效文字选区恢复可操作。点正文改变选区后不再强拉回。
- 交付：修清理/恢复时序，J11 用正常点击、键盘和 DOM 可见结果验证，不再通过 Vue exposed selectText 私下修复产品。
- 验收：主/副栏、同/异章、长文、关闭/返回原处、来源删除/内容变化；定位后可加批注且落在正确字符，scrollTop 不跳回。J11 连跑 10 次并保留分母；所有 stale 路径零误写。

### U04 · 试演可达性与状态闭环（目标，承接 A2-3/A3）

- 入口：BlockComposer、BlockDraft、SceneLaboratory、AdoptionImpact、页面 entry handlers。
- 不重做展开/折叠决策。统一“直接试写”与“先看走向”的去向和目标，切换保留作者输入；补已证实的布局/返回/状态缺口，不强制改所有组件。
- 当前场的正式纠正、安排下一段、仅本次参考分别显示实际作用；无世界书可直接试写。生成中只显示真实阶段和停止，不造百分比。
- 验收：1440/1024/390/360、720×450，深浅；输入和主按钮可通过正常滚动/键盘到达，手机底 rail 不盖住操作。覆盖取消/迟到/stale/失败/保存失败/只重试保存；统计模型请求数，直接试写不新增必经规划轮次。
- 一张静态截图不算过：必须走“写要求→试稿→手改→采用→撤销”与“停止→保留输入”。

### U05 · 作者追加一句要求，再试一次（目标，承接 A4-1/2）

- 样板：总册被动过，莉娜要保留秘密，同时维持与艾德加的关系；另有物理受限场景和无世界书普通稿，共三类。
- 复用现有方向的 action/gain/cost：2–3 项必须在行动/信息分配/代价上不同，不只是换语气；不另加长篇对白字段。
- 选中后提供一条追加要求，例如“但不要让艾德加离场”。通过**现有 instruction 接缝**形成新的受控请求，绑定原方向引用和新的 target revision；不原地修改 frozen direction，不复用旧采用许可。
- 保留当前旧稿，新请求取消/失败不能抹掉它；切回旧稿也必须重新检查来源/目标。仅保留当前与上一份内存稿，不做无限分支历史或新持久化真源。
- 验收：mock 响应由实际传入要求驱动，不能无论输入什么都返回“改好了”；检查请求确含追加要求，取消保旧稿、来源变化拒采用、双击不重复生成/写入；改要求只跑必要生成，不暗中再出一组方向。
- 若现有 session 无法安全表达，先交付受控意图接缝与离线合同，不用新增 shared 协议凑效果；把 UI 上线标 blocked，转储备。真实趣味性待晨间盲读，不称模型质量已提升。

### U06 · 样板与最终验证（目标，也是所有交付的收口）

- 三类样板各展示初始方向、作者一句修改、结果差异、采用/放弃；明确 mock/真实标签。每类保留一条可复跑路径。
- 全部 local journey + acceptance/V2 + 受改 F1/F2 Gate；列出跳过/失败与原因，不能把 mock J10 和真实 J7 混报。
- 最后 `verify:full`；最多 8 张代表图，包含状态/窄屏/正文标题，不上传成百张过程图充交付量。

### U 储备队列（主包提前完或局部阻断时执行）

| 包 | 前置与实质成果 | 验收/边界 |
|---|---|---|
| U-R1 标题与键盘/小高度专项 | 可独立；检查长书/章名、双栏保存/交换/关闭、390、560×450、720×450，修复实测抢空间或焦点不可达项 | 主标题可辨认且有完整名称查看方式；状态不挤占标题；键盘开关后回原栏，无隐形焦点；无问题则交具名证据，不虚假重构 |
| U-R2 试演差异离线评测 | 可独立；编制三类 × 正常/改要求/冲突/缺依据的预期行为，调用已有 planner/run 合同，报告同义重复和约束传递丢失 | deterministic seed、每例断言、错误注入；只测合同/交互，不把预写故事当真实模型评测 |
| U-R3 一份试稿留作构思 | U05 稳定后；复用既有 exploration repository、IdeaShelf 和来源 refs，显式保存一份未采用稿供再打开 | 原章 hash 不变、失败不丢稿、来源删除仍可读但旧采用许可失效；字段不够则只交映射差距，禁止另建存储/分支树 |

## 6. K 队列：真实接缝，而非更多孤立函数

顺序 K21 → K22 → K23 → K24 → K25 → K26。K-R 包不依赖 U；K24 前是强制技术门禁。主包容量参考 5–9h，储备约 3–6h；若不能接线，交付明确可接/不可接证据，不自封整个历史系统完成。

### K21 · 旧形状与授权清单签实（必做）

- 入口：现有 read model、F2 `authoringKnowledgeQuerySession` 的 repository snapshot/catalog、已有 evidence envelope。
- 逐字段列出：谁读、谁写、project/worldbook 归属、sourceRef、revision、时间/视角支持程度。用正式 reader/adapter 接口创建合成旧形状，不复制 rich fixture 当“真实旧档”。
- 验收：两个项目共用世界书、同名实体、重复历史 ID、显式旧地点 alias、tombstone、未绑定/换绑分别有结果；缺绑定或作用域不猜 ID。无新增正文扫描器、全局 activeWorldbook 切换或存储写入。

### K22 · 快照与输出安全硬化（必做）

- 入口：`createKnowledgeScope`、`freezeKnowledgeSnapshot`、identity/time/query、diagnostics/dependencies。
- 明确调用方与模块的冻结责任；可选择防御性复制后冻结或拒绝不合规输入，但不能悄悄 freeze 用户正在编辑的原对象。scope 生命周期不能持有旧 alias index 却读新内容。
- 覆盖 request/context 项目冲突、跨书共享、隐藏来源经错误/数量/ID 泄漏、Unicode/长 refs/诊断预算、零/负/极大预算、异常时间端点、预取消与中途取消、来源新增后依赖失效。
- 验收：输入前后 hash 一致，业务写入/真实网络 0；安全过滤先于排序/裁剪。输出预算按合同精确说明包含哪些字段，不能只截正文却漏掉无界 metadata。发现问题先加失败例再修。

### K23 · F2 evidence 桥接（必做，尚不接生产）

- 新增窄适配模块建议 `src/services/project/knowledgeReadModel/authoringEvidenceBridge.js`；将**已有授权 catalog 的明确子集**映射为 K 输入，再把允许结果映射回已有 evidence 格式。
- `history-node:` 与 `history:` 必须经项目/世界书/具体对象映射，禁止只换前缀；query 所指实体来自可信目标/来源集合，不从模型自报身份扩大范围。
- facts、excerpts、hypotheses 不混排升级；无时点/角色知识资料通过现有 missingInformation/不足说明表达。不能映射的来源 typed 拒绝，不伪装 worldbook-entry。
- 验收：round-trip 来源定位与 revision、同一来源去重、预算裁剪、来源删除/换绑、旧稿隔离；legacy/rich 分开报。给出与旧 F2 的差异表，指出接入实际增加了什么，不用重复回答冒充收益。

### K24 · 受限 I0 接入（目标，门禁通过才能做）

本计划获批准执行时，仅批准下述窄 I0 工程接缝；这不是全量 K 合同、历史工具或新权限的签收。启动 owner 必须先在任务板确认该范围和 K 写集；未确认则只做 K23 与储备，不临时修改上位合同。

- K21–K23 全过且桥接有实际收益后，由 K 作为唯一 owner 修改现有 F2 session 的准备/对账接缝，默认关闭新能力，保留原路径。U 不修改这些文件，故不需要等 U 整夜结束。
- **仅作者视角、已有 F2 授权集合内的精确世界书/历史资料**。不开放角色秘密问答、任意历史时点、未来正文、未采纳假设、整库动态工具查询，不把 I0 顺手升级 I1。
- 默认关闭时无新增读取、生成请求或行为变化；使用现有开关约定或明确的默认 false 依赖参数，测试可启用，不新增设置页。若需要无法容纳的 shared/schema 修改，阻断 I0 并转 K-R，不自行扩合同。
- 不增加第二套 provider 调用：结果继续进入既有 evidence envelope、源定位和 stale 对账；仅实际进入 prompt 的证据可标已参考。
- 权限/无效来源被拒时不能通过旧路径偷偷重新补全同一被拒资料。运行故障可回退原路径，但必须保持原授权范围并标明降级。
- 验收：开/关同一 fixture 对照；正常查询、未知、拒绝、无绑定、换书、换绑、关闭、迟到、来源删除/修改；实际 prompt、来源列表、debug 均不泄漏；原 F2 不回归。

### K25 · 接入生命周期与性能（目标）

- 覆盖 liveSource 未保存正文、主/副章、through-target 与 whole-book 分离、项目 revision 变化、缓存/scope 销毁。无明确 cutoff 的源不加入有 cutoff 的回答，不能以 excerpt 绕过。
- 100/1k/10k 样例测“reader/clone/freeze/scope/query/bridge”分阶段冷/热成本；I0 启用与关闭对照，记录查询 p50/p95、序列化大小和内存口径。复用现有预算，不新增 Worker/全局缓存掩盖问题。
- 验收：K 原 eval + 新桥接矩阵全过，1k 有界查询 p95 目标 ≤100ms；不达标先给瓶颈与回退结论。独立浏览器源跑既有 F2 资料助手 Gate，记录开/关结果。

### K26 · 对作者可验收的交付（目标，也是收口）

- 不新增 UI：给出在现有资料助手中“查一条已授权资料→查看原出处→改掉来源后旧答案失效”的隔离演示；能力开关默认仍关闭，晨间再决定是否启用。
- 三条可复跑命令：正常来源、未知时间、拒绝越权；源未变/已变各一例。无真实模型时用明确 mock，展示的是证据闭环而非回答质量。
- 全量 verify、K eval/benchmark、F2 受影响 Gate；I0 未成则交 K21–23 的精确模块和阻断，不伪造“已接入”。

### K 储备队列

| 包 | 前置与实质成果 | 验收/边界 |
|---|---|---|
| K-R1 变形/攻击性矩阵 | K22 后；确定 seed 的字段删减、顺序置换、跨项目 ID 注入、无关秘密增加、重复来源和预算缩小 | 顺序不改变授权结果，加入秘密不改变可见内容/低敏诊断，预算缩小不扩大输出；失败保留最小输入，不拿 case 数量当价值 |
| K-R2 真实源读取成本与取消 | 可先离线；区分 repository 全量读、快照复制、索引、查询成本，做可取消/销毁和重复 prepare 的有界基准 | 无新持久缓存/Worker/存储格式；只修已测得的重复计算或残留引用；优化前后同结果及 source revisions |
| K-R3 历史账本 H0 决策包 | K21 后；围绕“同一事件作者改写两次、故事时间不变、角色后知情、撤回确认”列 append-only/修订关系方案与迁移影响 | 一份可评审 ADR 草案和失败场景表；不写数据库、不取消 runtimeEvents 上限、不把未确认 Utopia 对象当架构真源；该包是储备，不替代可做的 K24 |

## 7. 文件所有权与禁止扩张

| 所有者 | 可写 | 不可写 |
|---|---|---|
| U | Authoring.vue / block-native.css、WritingNotebookEditor、BlockComposer/Draft/SceneLaboratory/AdoptionImpact/IdeaShelf、既有 scene laboratory run/方向 planner 接缝；必要探索文档调用方；journey runner/mock、F1/双栏/搜索 Gate；既有 authoringTurnComposer、uiControlContract、authoringTurnRuntime 测试 | knowledgeReadModel、F2 knowledge session/assistant/evidence bridge、worldStore、shared 合同、provider registry、持久化 schema |
| K | knowledgeReadModel、独立 K eval/bridge/benchmark；K24 通过后仅 authoringKnowledgeQuerySession 及同目录最小 F2 接缝；f2-knowledge-assistant-check；既有 authoringAgentWorkflows 测试 | Authoring.vue、Notebook、U 组件、shared 合同、工具注册、store、schema、package/lock、U 的 runner/mock |
| 集成 owner | board、STATUS/PLAN/LOG、正式合并与跨写集裁决 | 不与 worker 并写同一业务文件 |

需要新文件时放本线目录并登记；需要对方文件时写依赖请求，未获 owner 交接前不能碰。必要共用测试 helper 只由 owner 改一次，另一线可先使用本线局部 fixture。

核心测试预算仍是 **20 文件/200 用例**。断言并入受影响既有用例；显式脚本用于矩阵/实验，不靠藏测试规避预算。技能：改 UI 用 ui-style-check；改世界书/上下文用 worldbook-workflow；完成前 testing-verification；共享状态 docs-status-handoff；提交 commit-conventions。

本夜不做：真实付费模型/联网素材、I1 历史 tool call、角色知识数据库、永久历史写入、移动目录大重构、整页重写、全仓库清理、生产部署、main/server-version 合并或 push。真实渠道、费用上限、手机布局取舍、视觉冻结在晨间解决，不阻断已列独立任务。

## 8. 完成条件、交接与晨间验收

worker 交付 `docs/agent-runs/<run-id>/<u|k>/{progress,summary,verification}.md`；图片/JSON/log 在自身 worktree 的 tmp。摘要不超过约 1200 中文字，但测试详情必须可追溯。

- summary：实际完成/没完成；作者能感知什么；安全遗留；第一条验收动作；下一包。
- progress：每包状态、时间、写集、转向原因，主包/储备不得省略；有效时间与等待分开，无法测准就写未知。
- verification：BASE、最终代码 tree、最终 HEAD、命令/exit/分母、mock 拦截计数、来源根与端口、截图、所有失败。报告写完后只允许文档补记；再改代码必须重验。
- 交付前本地提交按 concern 收口，不堆 WIP；如果有未提交文件，列全 tracked/untracked 及原因，不能交一个 SHA 却漏掉实际页面代码。不得把失败藏到“适配债务”后称全绿。
- 结束前停止自己的测试进程，释放本线写锁并标 `frozen`。冻结后不在原目录悄悄再改；新想法写 deferred，晨间新开包。

**晨间先看失败，再看截图，约 25–35min：**

1. 5min：核对两线冻结、HEAD/tree/工作区、主包/储备状态；发现仍有写入就不合。
2. 10min U：长标题双栏→查找结果→关闭→加批注；再跑“要求→方向→追加一句→取消保旧稿/采用撤销”，看390和720×450。
3. 10min K：默认关闭旧路径仍可用；隔离启用后看正常来源、未知、拒绝、改来源后失效。确认 legacy/rich 差距，不要求用户读整个查询引擎。
4. owner 串行合并 U、K 到独立候选，检查写集冲突，重跑 full + I0/F2/Journey 交叉回归；两线独立绿色不等于合成绿色。没有用户明确请求不 push。
5. 再决定：新视觉是否接受、I0 是否允许启用、真实模型样本/费用、手机目录详情方向。自动化通过不替代这些判断。

## 9. 可直接派发的短指令

### 给 U

执行本任务书 U 队列。先验证 owner 给出的 base、独立 worktree、plan hash、截止和 mock 网络隔离；不得使用旧共享目录。按 U01→U02/U03→U04→U05→U06 推进，必做完成后继续目标及所有就绪储备，局部阻断按依赖转向。保留展开 composer、Worldbook 内容优先和手机堆叠，不做审美反复。交付稳定输入/搜索返回和作者追加一句再试的完整闭环。T+6h 功能冻结，最终按代码 tree 重验并提交，列出所有失败/未完成；停止自己的进程和写入，不合 main、不 push。

### 给 K

执行本任务书 K 队列。K0–K4 已存在，不重做；按 K21–K26 从旧源/权限/生命周期推进到 F2 默认关闭的窄 I0。只有 owner 确认 §6 范围且 K21–23 全过才接线；仅作者视角、已有授权证据，禁止伪装 source kind 或扩全库查询。受阻转 K-R 队列，不以写 H0 取代可做的接缝。保留旧路径、来源定位和 stale 门禁，default-off 无额外读取/请求。交付正常/未知/拒绝/来源修改四种演示及离线矩阵，T+6h 冻结并最终复验，不写 UI/main/shared，不 push。

### 给启动/集成 owner

先落实 §3，不只转发提示词。记录具体时间和工作区，确认 K24 的窄授权范围，运行一次续接自检；不满足条件不得声称整夜已启动。只维护共享任务板和解决写集交接，拒绝夜间扩权。检查每线是否有就绪包却提前退出，要求继续已授权队列；保留真实时间和失败。晨间先核对 frozen 与最终树，再独立验收和合并。

## 10. 本轮计划验证

本文是下一轮范围与验收依据，不是已实现/已运行的证明。本轮只改文档，不创建 worker/worktree、不启停服务、不请求真实模型。完成时在 LOG 记录实际文档验证结果。
